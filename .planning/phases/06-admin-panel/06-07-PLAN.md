---
phase: 06-admin-panel
plan: "07"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/(admin)/admin/reviews/page.tsx
  - src/app/(admin)/admin/settings/page.tsx
  - src/app/(admin)/_components/ReviewForm.tsx
  - src/app/(admin)/_components/SettingsForm.tsx
  - src/app/api/admin/reviews/route.ts
  - src/app/api/admin/settings/route.ts
autonomous: true

must_haves:
  truths:
    - "Admin can add a customer review to any product by selecting the product and filling in author name, body, and star rating"
    - "Saved review appears immediately on the storefront product detail page"
    - "Admin can edit all settings: store name, tagline, email, WhatsApp number, shipping flat rate, Instagram, TikTok, Facebook URLs"
    - "Saving settings updates the Supabase settings table — changes take effect on the storefront at next page load with no redeploy"
    - "WhatsApp number in settings is used by the storefront — no hardcoded env var fallback needed after this plan"
  artifacts:
    - path: "src/app/(admin)/admin/reviews/page.tsx"
      provides: "Review entry page — Server Component"
      min_lines: 20
    - path: "src/app/(admin)/_components/ReviewForm.tsx"
      provides: "Review create form with product picker"
      min_lines: 50
    - path: "src/app/(admin)/admin/settings/page.tsx"
      provides: "Business settings page — Server Component"
      min_lines: 20
    - path: "src/app/(admin)/_components/SettingsForm.tsx"
      provides: "Settings form — all key-value fields"
      min_lines: 60
    - path: "src/app/api/admin/reviews/route.ts"
      provides: "POST /api/admin/reviews"
      exports: ["POST"]
    - path: "src/app/api/admin/settings/route.ts"
      provides: "PUT /api/admin/settings — upsert key-value rows"
      exports: ["PUT"]
  key_links:
    - from: "src/app/(admin)/_components/ReviewForm.tsx"
      to: "/api/admin/reviews"
      via: "fetch POST on submit"
      pattern: "api/admin/reviews"
    - from: "src/app/(admin)/_components/SettingsForm.tsx"
      to: "/api/admin/settings"
      via: "fetch PUT on save"
      pattern: "api/admin/settings"
    - from: "src/app/api/admin/settings/route.ts"
      to: "adminClient.from('settings').upsert()"
      via: "upserts all key-value pairs"
      pattern: "settings"
---

<objective>
Build the reviews entry form and business settings form. Both are simple forms backed by service-role API routes. Settings changes take effect on the storefront at next page load.

Purpose: Unoma can add customer reviews to products and update business details (WhatsApp, social links, shipping rate) without touching code or redeploying.
Output: /admin/reviews + /admin/settings pages fully functional.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-admin-panel/06-CONTEXT.md
@.planning/phases/06-admin-panel/06-RESEARCH.md
@.planning/phases/06-admin-panel/06-01-SUMMARY.md
@src/types/supabase.ts
@src/lib/supabase/admin.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reviews API route + review form page</name>
  <files>
    src/app/api/admin/reviews/route.ts
    src/app/(admin)/admin/reviews/page.tsx
    src/app/(admin)/_components/ReviewForm.tsx
  </files>
  <action>
    CREATE src/app/api/admin/reviews/route.ts — POST handler:
    - Auth check: createClient + getUser — 401 if no user
    - Parse body: { product_id: string; author_name: string; body: string; rating: number }
    - Validate: product_id non-empty, author_name non-empty, body non-empty, rating between 1 and 5
    - Use createAdminClient() to insert into reviews table
    - Return 201 with created review or 400/500 on error

    CREATE src/app/(admin)/admin/reviews/page.tsx — Server Component:
    - Auth check
    - Fetch all products (id, name) for the product picker dropdown using createAdminClient()
    - Render: heading "Add Review" + <ReviewForm products={products} />

    CREATE src/app/(admin)/_components/ReviewForm.tsx — 'use client':
    - Props: products: Array<{ id: string; name: string }>
    - Fields:
      - Product: <select> showing all product names; value = product id
      - Author name: text input (placeholder: "e.g. Adaeze O.")
      - Review text: <textarea> min 3 rows
      - Star rating: 1–5 star selector (5 clickable star buttons using ★/☆ icons, or radio inputs styled as stars)
    - Submit: POST to /api/admin/reviews; on success show "Review saved" toast + reset all fields; on error show inline error
    - Use useTransition for pending state
    - Simple toast (same pattern as ProductForm — useState + setTimeout 3s)
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>API route compiles. ReviewForm renders all fields and submits correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Settings API route + settings form page</name>
  <files>
    src/app/api/admin/settings/route.ts
    src/app/(admin)/admin/settings/page.tsx
    src/app/(admin)/_components/SettingsForm.tsx
  </files>
  <action>
    CREATE src/app/api/admin/settings/route.ts — PUT handler:
    - Auth check: createClient + getUser — 401 if no user
    - Parse body: Record<string, string> (an object of key -> value pairs)
    - For each key-value pair, upsert into settings table using adminClient.from('settings').upsert({ key, value }, { onConflict: 'key' })
    - Can batch: map to array of { key, value } objects and do a single upsert call
    - Return 200 { updated: keys[] } on success

    CREATE src/app/(admin)/admin/settings/page.tsx — Server Component:
    - Auth check
    - Fetch all settings using createAdminClient().from('settings').select('key, value')
    - Convert to Record<string, string>: Object.fromEntries(rows.map(r => [r.key, r.value]))
    - Render: heading "Settings" + <SettingsForm settings={settings} />

    CREATE src/app/(admin)/_components/SettingsForm.tsx — 'use client':
    - Props: settings: Record<string, string>
    - Initialise local state: all settings fields individually for clean controlled inputs
    - Fields (grouped into sections):

    Store Details:
    - Store name (key: store_name) — text input
    - Tagline (key: store_tagline) — text input
    - Email (key: email) — email input
    - Address (key: address) — textarea

    Contact & Business:
    - WhatsApp number (key: whatsapp_number) — text input; help text "Include country code, e.g. +2349118888010"
    - Shipping flat rate ₦ (key: shipping_flat_rate) — number input; help text "Applied to all orders (override per-state logic not supported in v1)"

    Social Links:
    - Instagram URL (key: instagram_url) — url input
    - TikTok URL (key: tiktok_url) — url input
    - Facebook URL (key: facebook_url) — url input

    Save button at bottom: PUT to /api/admin/settings with all current field values as a single object
    On success: toast "Settings saved"
    On error: toast "Error saving settings"
    Use useTransition for pending state

    Section headings: text-stone-400 uppercase text-xs tracking-wider, divider line below
    Input styling: consistent with ProductForm — bg-stone-800 border-stone-600 text-white rounded
  </action>
  <verify>
    npm run build
    Visit http://localhost:3000/admin/reviews — product picker and review fields render.
    Add a review for a product — navigate to the storefront product page and confirm the review appears.
    Visit http://localhost:3000/admin/settings — all fields pre-filled with current settings.
    Change WhatsApp number, save — verify Supabase settings table reflects the new value.
  </verify>
  <done>
    Review form saves to Supabase and review appears on storefront product detail page.
    Settings form pre-fills from DB, saves changes to settings table on submit.
    WhatsApp number change in settings reflects immediately on the storefront (next page load).
    No TypeScript errors. No console errors.
  </done>
</task>

</tasks>

<verification>
1. npm run build — zero errors
2. Add a review via /admin/reviews — review appears on /catalog/[product-slug] product detail
3. Update WhatsApp number in /admin/settings — verify new number in Supabase settings table
4. Update store name — verify storefront reads from settings table (if wired; if not, note as future work)
5. All form fields pre-fill from DB values on page load
</verification>

<success_criteria>
Unoma can enter customer reviews and update all business settings from the admin panel. No code changes or redeployment needed for these operations.
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-panel/06-07-SUMMARY.md`
</output>
