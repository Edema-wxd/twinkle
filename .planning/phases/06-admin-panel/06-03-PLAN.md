---
phase: 06-admin-panel
plan: "03"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/(admin)/admin/products/page.tsx
  - src/app/(admin)/_components/ProductListTable.tsx
  - src/app/catalog/page.tsx
autonomous: true

must_haves:
  truths:
    - "Admin can see all products in a table with name, category, status (active/archived), and actions"
    - "Admin can search products by name — table filters to matching products instantly"
    - "Admin can filter products by category (All / Gold / Silver / Crystal / Tools)"
    - "Admin can toggle a product between active and archived — change persists and takes effect on storefront"
    - "Active products appear on /catalog; archived products do not"
    - "Admin can click 'Edit' on a product row to navigate to its edit form"
    - "Admin can click 'New product' to navigate to the new product form"
  artifacts:
    - path: "src/app/(admin)/admin/products/page.tsx"
      provides: "Product list page — Server Component, fetches all products"
      min_lines: 30
    - path: "src/app/(admin)/_components/ProductListTable.tsx"
      provides: "Interactive product table with search, filter, archive toggle"
      min_lines: 80
    - path: "src/app/catalog/page.tsx"
      provides: "Storefront catalog filtered by is_active = true"
      contains: "is_active"
  key_links:
    - from: "src/app/(admin)/_components/ProductListTable.tsx"
      to: "/api/admin/products/[id]/toggle-active"
      via: "fetch PATCH on archive toggle"
      pattern: "toggle-active"
    - from: "src/app/(admin)/admin/products/page.tsx"
      to: "createAdminClient()"
      via: "fetches all products including inactive"
      pattern: "createAdminClient"
    - from: "src/app/catalog/page.tsx"
      to: "supabase.from('products').eq('is_active', true)"
      via: "storefront query filter"
      pattern: "is_active"
---

<objective>
Build the product list admin page: a searchable, filterable table of all products with archive toggle, edit links, and a "New product" button. Also add is_active filter to the storefront catalog.

Purpose: Gives Unoma visibility and control over her product catalogue without touching code.
Output: Working /admin/products page + storefront is_active filtering.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-admin-panel/06-CONTEXT.md
@.planning/phases/06-admin-panel/06-01-SUMMARY.md
@src/lib/supabase/admin.ts
@src/types/supabase.ts
@src/app/catalog/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Toggle-active API route + storefront is_active filter</name>
  <files>
    src/app/api/admin/products/[id]/toggle-active/route.ts
    src/app/catalog/page.tsx
  </files>
  <action>
    CREATE src/app/api/admin/products/[id]/toggle-active/route.ts — PATCH handler:
    - Import createClient from server.ts; call getUser(); return 401 if no user
    - Import createAdminClient from admin.ts
    - Read the product's current is_active from DB using adminClient
    - PATCH to set is_active = !currentValue
    - Return 200 with { is_active: newValue }
    - Return 404 if product not found, 500 on error

    UPDATE src/app/catalog/page.tsx:
    - Find the Supabase query that fetches products
    - Add .eq('is_active', true) to the query so archived products never appear on the storefront
    - No other changes to this file — preserve all existing sorting/filtering logic
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>API route exists and type-checks. Catalog query has is_active filter.</done>
</task>

<task type="auto">
  <name>Task 2: Product list page + ProductListTable component</name>
  <files>
    src/app/(admin)/admin/products/page.tsx
    src/app/(admin)/_components/ProductListTable.tsx
  </files>
  <action>
    CREATE src/app/(admin)/admin/products/page.tsx — Server Component:
    - Independently verify auth (createClient + getUser + redirect pattern, same as layout.tsx)
    - Fetch all products using createAdminClient(): select('id, name, slug, material, is_active, is_featured, price_min, price_max, created_at') — NOT filtered by is_active (admin sees everything)
    - Order by created_at descending
    - Render: page heading "Products" + "New product" button linking to /admin/products/new + <ProductListTable products={products} />

    CREATE src/app/(admin)/_components/ProductListTable.tsx — 'use client':
    - Props: products: Array<{ id: string; name: string; slug: string; material: string; is_active: boolean; price_min: number; price_max: number; created_at: string }>
    - Local state: searchQuery (string), activeCategory ('All'|'Gold'|'Silver'|'Crystal'|'Tools'), optimistic is_active per product (Record<string, boolean> initialised from props)

    Search: filter products where name.toLowerCase().includes(searchQuery.toLowerCase())
    Category filter: if activeCategory !== 'All', filter by material === activeCategory (applied after search)
    Combined filter: both filters applied with useMemo

    Render:
    - Search input (type="search", placeholder="Search products...") — full width on mobile, w-64 on md+
    - Category filter chips: All / Gold / Silver / Crystal / Tools — same style as storefront FilterBar
    - Count: "{N} products"
    - Table columns: Name | Category | Price range | Status | Actions
    - Status badge: "Active" (green pill) or "Archived" (stone/grey pill) — driven by optimistic state
    - Archive toggle button: "Archive" (if active) or "Restore" (if archived) — calls PATCH /api/admin/products/{id}/toggle-active; on success update optimistic state; on error show alert with error message
    - Edit button: Link to /admin/products/{id}
    - "New product" button also appears at the top right above the table (same as page-level button — redundant on mobile but helpful on desktop)
    - Empty state: "No products match your search" if filtered list is empty

    Table styling: bg-stone-900 rows, hover:bg-stone-800, border-stone-700 dividers. Responsive: on mobile collapse to card layout or use horizontal scroll with min-w-max on table.
  </action>
  <verify>
    npm run build
    Visit http://localhost:3000/admin/products — table renders all products.
    Type in search box — list filters.
    Click a category chip — list filters by material.
    Click Archive on one product — status badge changes to Archived.
    Visit http://localhost:3000/catalog — archived product does not appear.
  </verify>
  <done>
    /admin/products shows all products including archived ones.
    Search and category filter work client-side without page reloads.
    Archive toggle persists to DB and removes product from storefront /catalog.
    No TypeScript errors. No console errors.
  </done>
</task>

</tasks>

<verification>
1. npm run build — zero errors
2. /admin/products renders product table with all products
3. Search input filters table in real-time
4. Category chips filter by material
5. Archiving a product removes it from /catalog
6. Restoring an archived product makes it reappear on /catalog
7. "New product" and "Edit" links exist (routes 06-04 will implement the forms)
</verification>

<success_criteria>
Admin can search, filter, and archive products from the list view. Storefront catalog is gated by is_active.
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-panel/06-03-SUMMARY.md`
</output>
