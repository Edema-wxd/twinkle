---
phase: 06-admin-panel
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/(admin)/admin/page.tsx
  - src/app/(admin)/_components/StatsPanel.tsx
  - src/app/(admin)/_components/RecentOrdersTable.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard shows total sales in ₦ for three date ranges: Today, This week, This month"
    - "Dashboard shows order count for the selected date range"
    - "Dashboard shows a table of the last 10 orders with: order #, customer name, date, total, status"
    - "Active tab (Today / This week / This month) is visually highlighted"
    - "Dashboard data comes from real Supabase orders — not hardcoded"
  artifacts:
    - path: "src/app/(admin)/admin/page.tsx"
      provides: "Dashboard page — Server Component fetching orders data"
      min_lines: 40
    - path: "src/app/(admin)/_components/StatsPanel.tsx"
      provides: "Stats display: total sales + order count with date tabs"
      min_lines: 40
    - path: "src/app/(admin)/_components/RecentOrdersTable.tsx"
      provides: "Table of last 10 orders"
      min_lines: 30
  key_links:
    - from: "src/app/(admin)/admin/page.tsx"
      to: "src/lib/supabase/admin.ts"
      via: "createAdminClient() fetch of orders table"
      pattern: "createAdminClient"
    - from: "src/app/(admin)/_components/StatsPanel.tsx"
      to: "src/app/(admin)/admin/page.tsx"
      via: "orders prop passed from page"
      pattern: "orders"
---

<objective>
Build the admin dashboard: a stats panel showing total sales and order count for Today / This week / This month, plus a table of recent orders.

Purpose: Gives Unoma an at-a-glance view of business performance each time she opens the admin panel.
Output: A real, data-driven dashboard page replacing the 06-01 stub.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-admin-panel/06-CONTEXT.md
@.planning/phases/06-admin-panel/06-RESEARCH.md
@.planning/phases/06-admin-panel/06-01-SUMMARY.md
@src/lib/supabase/admin.ts
@src/types/supabase.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stats data fetching helpers</name>
  <files>
    src/app/(admin)/admin/page.tsx
  </files>
  <action>
    REPLACE the stub dashboard page with a full Server Component:

    Data fetching pattern:
    - Call createAdminClient()
    - Independently verify auth: createClient() from server.ts + getUser() — redirect to /admin/login if no user
    - Fetch ALL orders from the orders table: select('id, created_at, status, customer_name, total, paystack_reference').order('created_at', { ascending: false })
    - Compute stats in-component (no separate utils file needed):

    Date range computation:
    - today: Date where getHours/Minutes/Seconds/Ms are set to 0 (start of current day in local time)
    - weekStart: today minus today.getDay() days (start of current week, Sunday)
    - monthStart: new Date(today.getFullYear(), today.getMonth(), 1)

    Derive three stat objects from the fetched orders array:
    - todayOrders: filter where created_at >= today ISO string
    - weekOrders: filter where created_at >= weekStart ISO string
    - monthOrders: filter where created_at >= monthStart ISO string

    Each stat object: { count: number, totalSales: number } computed with array reduce.

    Pass to components:
    - <StatsPanel stats={{ today: {...}, week: {...}, month: {...} }} />
    - <RecentOrdersTable orders={orders.slice(0, 10)} />
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>page.tsx compiles, fetches from Supabase orders table, passes typed props to child components.</done>
</task>

<task type="auto">
  <name>Task 2: StatsPanel + RecentOrdersTable components</name>
  <files>
    src/app/(admin)/_components/StatsPanel.tsx
    src/app/(admin)/_components/RecentOrdersTable.tsx
  </files>
  <action>
    CREATE src/app/(admin)/_components/StatsPanel.tsx — 'use client' (needs tab state):
    - Props: stats: { today: { count: number; totalSales: number }; week: { count: number; totalSales: number }; month: { count: number; totalSales: number } }
    - Three tabs: "Today" | "This week" | "This month" — use useState to track activeTab
    - Display for active tab:
      - "Total Sales" card: format totalSales as ₦XX,XXX (use toLocaleString('en-NG') or manual format with commas)
      - "Orders" card: show count
    - Tab bar styling: active tab has bg-amber-600 text-white; inactive has bg-stone-800 text-stone-300
    - Cards: bg-stone-800 rounded-lg p-6, white heading, large number in brand gold (text-amber-500)
    - Two stat cards side-by-side on md+, stacked on mobile

    CREATE src/app/(admin)/_components/RecentOrdersTable.tsx — Server Component (no interactivity):
    - Props: orders: Order[] (import Order from @/types/supabase)
    - Renders a table with columns: Order # (paystack_reference last 8 chars) | Customer | Date | Total | Status
    - Date formatted: toLocaleDateString('en-NG', { day: 'numeric', month: 'short', year: 'numeric' })
    - Total formatted: ₦XX,XXX
    - Status badge: colour-coded pill — pending: yellow, processing: blue, shipped: purple, delivered: green, paid: amber
    - Responsive: on mobile, show Order # + Customer + Status only (hide Date and Total with hidden md:table-cell)
    - Empty state: "No orders yet" centred message if orders array is empty
    - Table header: bg-stone-800; rows: bg-stone-900 with hover:bg-stone-800 transition
  </action>
  <verify>
    npm run build
    Visit http://localhost:3000/admin — dashboard should render with stats panel and recent orders table.
  </verify>
  <done>
    Dashboard renders StatsPanel with three date-range tabs and two stat cards.
    Switching tabs updates the displayed stats.
    RecentOrdersTable shows up to 10 recent orders (or "No orders yet" if DB is empty).
    No TypeScript errors. No console errors.
  </done>
</task>

</tasks>

<verification>
1. npm run build — zero errors
2. /admin renders StatsPanel + RecentOrdersTable without console errors
3. Stats tabs switch correctly between Today / This week / This month
4. Orders table shows real data from Supabase (verify by checking at least one order matches a test order from Phase 5)
</verification>

<success_criteria>
Admin dashboard is functional with live data. Unoma can see her sales stats and recent orders at a glance.
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-panel/06-02-SUMMARY.md`
</output>
