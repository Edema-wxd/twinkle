---
phase: 06-admin-panel
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/supabase/admin.ts
  - src/types/supabase.ts
  - middleware.ts
  - src/app/(admin)/layout.tsx
  - src/app/(admin)/admin/login/page.tsx
  - src/app/(admin)/admin/login/actions.ts
  - src/app/(admin)/_components/AdminSidebar.tsx
  - src/app/(admin)/admin/page.tsx
autonomous: true
user_setup:
  - service: supabase
    why: "Admin user creation + Storage bucket — cannot be created by code"
    dashboard_config:
      - task: "Create admin user"
        location: "Supabase Dashboard -> Authentication -> Users -> Add user (confirm email checked)"
        details: "Email: the store owner's email. Password: choose a strong password. DO NOT create a public signup route."
      - task: "Create product-images storage bucket"
        location: "Supabase Dashboard -> Storage -> New bucket"
        details: "Name: product-images. Toggle Public ON. Then add RLS policies: Storage INSERT for authenticated; Storage DELETE for authenticated; Storage SELECT for public (anon)."
      - task: "Run SQL migrations"
        location: "Supabase Dashboard -> SQL Editor -> New query"
        details: |
          -- 1. Add is_active column (backfills existing rows as true)
          ALTER TABLE public.products
            ADD COLUMN IF NOT EXISTS is_active boolean NOT NULL DEFAULT true;

          -- 2. Create settings table
          CREATE TABLE IF NOT EXISTS public.settings (
            key   text PRIMARY KEY,
            value text NOT NULL DEFAULT ''
          );
          ALTER TABLE public.settings ENABLE ROW LEVEL SECURITY;
          CREATE POLICY "Settings are publicly readable"
            ON public.settings FOR SELECT USING (true);
          CREATE POLICY "Only authenticated users can update settings"
            ON public.settings FOR UPDATE USING (auth.role() = 'authenticated');

          -- 3. Seed initial settings
          INSERT INTO public.settings (key, value) VALUES
            ('store_name', 'Twinkle Locs'),
            ('store_tagline', 'Your locs, your crown'),
            ('address', ''),
            ('email', ''),
            ('whatsapp_number', '+2349118888010'),
            ('shipping_flat_rate', '1500'),
            ('instagram_url', 'https://instagram.com/twinklelocs'),
            ('tiktok_url', ''),
            ('facebook_url', '')
          ON CONFLICT (key) DO NOTHING;

          -- 4. Verify images column exists (Phase 4 added it; this is a safety check)
          ALTER TABLE public.products
            ADD COLUMN IF NOT EXISTS images text[] NOT NULL DEFAULT '{}';

must_haves:
  truths:
    - "Visiting /admin without a session redirects to /admin/login"
    - "Logging in with valid credentials at /admin/login redirects to /admin"
    - "Logging in with wrong password shows an inline error message"
    - "Logging out from /admin redirects to /admin/login"
    - "Admin pages render the sidebar and header — storefront sidebar never appears"
  artifacts:
    - path: "src/lib/supabase/admin.ts"
      provides: "createAdminClient() using SUPABASE_SERVICE_ROLE_KEY"
      exports: ["createAdminClient"]
    - path: "middleware.ts"
      provides: "Admin route guard using getUser()"
      contains: "isAdminPath"
    - path: "src/app/(admin)/layout.tsx"
      provides: "Admin shell: sidebar + header, isolated from storefront layout"
      min_lines: 30
    - path: "src/app/(admin)/admin/login/actions.ts"
      provides: "loginAction and logoutAction server actions"
      exports: ["loginAction", "logoutAction"]
    - path: "src/types/supabase.ts"
      provides: "is_active + settings types added"
      contains: "is_active"
  key_links:
    - from: "middleware.ts"
      to: "supabase.auth.getUser()"
      via: "admin guard block"
      pattern: "getUser"
    - from: "src/app/(admin)/admin/login/actions.ts"
      to: "supabase.auth.signInWithPassword"
      via: "loginAction server action"
      pattern: "signInWithPassword"
    - from: "src/app/(admin)/layout.tsx"
      to: "src/app/(admin)/_components/AdminSidebar.tsx"
      via: "import and render"
      pattern: "AdminSidebar"
---

<objective>
Establish the admin foundation: database schema migrations, Supabase service-role client, admin route guard in middleware, login/logout pages, and the admin layout shell with sidebar.

Purpose: Every subsequent plan (02-07) depends on auth being in place and the admin Supabase client existing. This is the blocker — nothing else can be built until the protected shell exists.
Output: Protected /admin shell with working login/logout; admin service-role client; is_active + settings schema in Supabase.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-admin-panel/06-CONTEXT.md
@.planning/phases/06-admin-panel/06-RESEARCH.md
@src/lib/supabase/server.ts
@src/lib/supabase/client.ts
@middleware.ts
@src/types/supabase.ts
@src/app/layout.tsx
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Supabase setup (admin user + storage bucket + SQL migrations)</name>
  <what-built>Nothing yet — this is human-only configuration that must precede code.</what-built>
  <how-to-verify>
    Complete these three steps in the Supabase dashboard before proceeding:

    STEP A — Create admin user
    1. Open Supabase Dashboard -> Authentication -> Users
    2. Click "Add user" (Invite user)
    3. Enter the store owner's email and a strong password
    4. Ensure "Auto Confirm User" is checked (or confirm via email link)
    5. Note the user's UUID — you will need it if you restrict the admin guard to specific UIDs later

    STEP B — Create product-images storage bucket
    1. Open Supabase Dashboard -> Storage -> New bucket
    2. Name: product-images
    3. Toggle "Public bucket" ON
    4. Save, then add these RLS policies on the bucket:
       - Allow authenticated INSERT: Authenticated users can upload
       - Allow authenticated DELETE: Authenticated users can delete
       - Allow public SELECT: Everyone can read (public bucket, but good to be explicit)

    STEP C — Run SQL migrations in Supabase SQL Editor
    Copy and run the SQL from the user_setup.dashboard_config block in this plan's frontmatter.
    Verify: SELECT column_name FROM information_schema.columns WHERE table_name = 'products' AND column_name = 'is_active';
    Should return one row.
  </how-to-verify>
  <resume-signal>Type "done" when all three steps are complete</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Admin Supabase client + TypeScript type additions</name>
  <files>
    src/lib/supabase/admin.ts
    src/types/supabase.ts
  </files>
  <action>
    CREATE src/lib/supabase/admin.ts — service-role Supabase client:
    - Export createAdminClient() that calls createClient<Database>() from @supabase/supabase-js (not @supabase/ssr — no cookies needed)
    - Use process.env.NEXT_PUBLIC_SUPABASE_URL and process.env.SUPABASE_SERVICE_ROLE_KEY
    - Throw a clear error if SUPABASE_SERVICE_ROLE_KEY is not set
    - Add a comment: "// NEVER import this file from a 'use client' component — service role key must not reach the browser"
    - No auth.persistSession option needed (service role clients don't have sessions)

    UPDATE src/types/supabase.ts — add three things:
    1. Add `is_active: boolean` to products.Row (after `is_featured`)
    2. Add `settings` table:
       Row: { key: string; value: string }
       Insert: { key: string; value: string }
       Update: { key?: string; value?: string }
       Relationships: []
    3. At the bottom, add convenience types:
       export type Product = Database['public']['Tables']['products']['Row']
       export type Setting = Database['public']['Tables']['settings']['Row']
       (Do not add Product type if it already exists — check first)
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>No TypeScript errors. admin.ts exports createAdminClient. supabase.ts has is_active on products.Row and a settings table definition.</done>
</task>

<task type="auto">
  <name>Task 3: Middleware admin guard + login/logout + admin layout shell</name>
  <files>
    middleware.ts
    src/app/(admin)/admin/login/actions.ts
    src/app/(admin)/admin/login/page.tsx
    src/app/(admin)/layout.tsx
    src/app/(admin)/_components/AdminSidebar.tsx
    src/app/(admin)/admin/page.tsx
  </files>
  <action>
    UPDATE middleware.ts — add Step 3 + Step 4 after the existing getClaims() call:

    Step 3 (admin guard): if pathname starts with /admin AND is NOT /admin/login:
    - Call supabase.auth.getUser() (NOT getClaims — this validates against auth server)
    - If no user, return NextResponse.redirect to /admin/login

    Step 4 (login redirect): if pathname is exactly /admin/login:
    - Call supabase.auth.getUser()
    - If user exists, return NextResponse.redirect to /admin

    Preserve the existing lowercase redirect (Step 1) and getClaims session refresh (Step 2) unchanged.
    The existing api/webhooks exclusion in config.matcher must remain.

    CREATE src/app/(admin)/admin/login/actions.ts — two server actions:
    - loginAction(formData: FormData): calls supabase.auth.signInWithPassword({email, password}); on error returns { error: error.message }; on success calls redirect('/admin')
    - logoutAction(): calls supabase.auth.signOut() then redirect('/admin/login')
    Both use the cookie-based createClient() from src/lib/supabase/server.ts (NOT admin client — auth operations use the user-facing client).

    CREATE src/app/(admin)/admin/login/page.tsx — login form:
    - 'use client' component
    - Form with email input, password input, submit button
    - Calls loginAction via useTransition (startTransition wrapping form action) OR use a <form action={loginAction}> — either pattern works
    - If loginAction returns { error }, display the error message below the form
    - Styled with Twinkle Locs palette: bg-stone-950 page, white card, brand gold accent on button
    - No admin sidebar — this page is outside the authenticated shell
    - Page title: "Twinkle Locs Admin"

    CREATE src/app/(admin)/_components/AdminSidebar.tsx — 'use client' sidebar:
    - Fixed left sidebar, full height, bg-stone-900 with white/gold text
    - Logo/brand name at top ("Twinkle Locs" in Halimun font, gold colour)
    - Nav links: Dashboard (/admin), Products (/admin/products), Orders (/admin/orders), Reviews (/admin/reviews), Settings (/admin/settings)
    - Active link highlighted (use usePathname() to detect)
    - Logout button at bottom — calls logoutAction server action via a <form> with a submit button
    - On mobile (< lg): sidebar is hidden; show hamburger menu icon in header that toggles a drawer. Use useState for isOpen.
    - On lg+: sidebar is always visible as a fixed left column

    CREATE src/app/(admin)/layout.tsx — admin shell layout:
    - NOT 'use client' — Server Component
    - Independently verify auth: call createClient() and getUser(); if no user, redirect('/admin/login'). This is the per-page auth check (CVE-2025-29927 — middleware alone is not sufficient).
    - Render: AdminSidebar (left) + main content area (right)
    - lg: flex layout with w-64 sidebar + flex-1 main
    - Mobile: full-width main, sidebar hidden (controlled by AdminSidebar's internal state)
    - Does NOT import CartProvider, Header, or Footer — completely isolated from storefront layout

    CREATE src/app/(admin)/admin/page.tsx — stub dashboard:
    - Server Component
    - Independently verify auth (same pattern as layout.tsx — belt and braces)
    - Render a heading "Dashboard" and a paragraph "Loading dashboard..." for now
    - Plan 06-02 will replace this stub with real stats
  </action>
  <verify>
    npm run build
    If build passes: curl -I http://localhost:3000/admin (with dev server running) — should see a redirect response (302 to /admin/login).
    Then visit http://localhost:3000/admin/login in browser — login form should render.
  </verify>
  <done>
    Unauthenticated GET /admin returns a redirect to /admin/login.
    /admin/login renders the login form with no console errors.
    Logging in with valid Supabase credentials lands on /admin (stub dashboard page).
    Logging in with wrong credentials shows an inline error message.
    Clicking logout redirects back to /admin/login.
    No storefront components (Header, CartProvider, Footer) appear on any /admin route.
  </done>
</task>

</tasks>

<verification>
1. npx tsc --noEmit — zero errors
2. npm run build — zero errors
3. Unauthenticated visit to /admin redirects to /admin/login
4. Valid credentials at /admin/login redirect to /admin
5. Invalid credentials show inline error, no redirect
6. Logout from /admin redirects to /admin/login
7. Supabase dashboard: products table has is_active column; settings table exists with 9 seed rows
8. products-images storage bucket exists and is public
</verification>

<success_criteria>
Protected /admin shell is operational. Admin can log in and see the sidebar and stub dashboard. All subsequent plans (06-02 through 06-07) can build on this auth + layout foundation.
</success_criteria>

<output>
After completion, create `.planning/phases/06-admin-panel/06-01-SUMMARY.md`
</output>
