---
phase: 02-homepage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/mock/products.ts
  - src/lib/mock/testimonials.ts
  - public/images/products/placeholder-bead.jpg
autonomous: true

must_haves:
  truths:
    - "Mock product data exists and is importable from src/lib/mock/products.ts"
    - "Mock testimonial data exists and is importable from src/lib/mock/testimonials.ts"
    - "A placeholder image exists at public/images/products/placeholder-bead.jpg so next/image does not 404"
  artifacts:
    - path: "src/lib/mock/products.ts"
      provides: "MockProduct type, MockProductVariant type, FEATURED_PRODUCTS array (4 items)"
      exports: ["MockProduct", "MockProductVariant", "FEATURED_PRODUCTS"]
    - path: "src/lib/mock/testimonials.ts"
      provides: "Testimonial type, TESTIMONIALS array (3 items)"
      exports: ["Testimonial", "TESTIMONIALS"]
    - path: "public/images/products/placeholder-bead.jpg"
      provides: "Placeholder product image for all 4 product cards"
  key_links:
    - from: "src/components/home/FeaturedProductsSection.tsx"
      to: "src/lib/mock/products.ts"
      via: "named import FEATURED_PRODUCTS"
      pattern: "import.*FEATURED_PRODUCTS.*from.*lib/mock/products"
    - from: "src/app/page.tsx"
      to: "src/lib/mock/testimonials.ts"
      via: "named import TESTIMONIALS"
      pattern: "import.*TESTIMONIALS.*from.*lib/mock/testimonials"
---

<objective>
Create the mock data layer for the homepage: typed product and testimonial arrays that the section components will import, plus a placeholder image so next/image renders without 404s.

Purpose: Phase 2 cannot query Supabase (products table does not exist until Phase 3). This plan creates the static data that makes featured products and testimonials possible. The MockProduct interface is designed to anticipate the Phase 3 Supabase schema so the swap in Phase 3 is a one-line change.

Output: Two TypeScript files in src/lib/mock/ and one placeholder image in public/images/products/.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-homepage/02-CONTEXT.md
@.planning/phases/02-homepage/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mock data files (products + testimonials)</name>
  <files>src/lib/mock/products.ts, src/lib/mock/testimonials.ts</files>
  <action>
Create the directory src/lib/mock/ and write two TypeScript files.

**src/lib/mock/products.ts** — Export MockProductVariant interface, MockProduct interface, and FEATURED_PRODUCTS array with exactly 4 products matching the real Twinkle Locs catalog:

MockProductVariant interface:
- id: string
- name: string  (e.g. "Small (4mm)")
- price: number  (in Naira, no kobo — e.g. 2500 means ₦2,500)
- in_stock: boolean

MockProduct interface:
- id: string
- name: string
- slug: string
- description: string
- image: string  (path to /public image — use "/images/products/placeholder-bead.jpg" for all 4)
- is_featured: boolean
- variants: MockProductVariant[]
- price_min: number  (lowest variant price)
- price_max: number  (highest variant price)

FEATURED_PRODUCTS array — 4 products from the real catalog:
1. id: "prod_001", name: "24K Gold Beads", slug: "24k-gold-beads", description: "Lustrous 24K gold-plated loc beads — the signature Twinkle Locs piece.", variants: Small (₦2,500), Medium (₦3,200), Large (₦4,000), is_featured: true
2. id: "prod_002", name: "Gold Beads", slug: "gold-beads", description: "Classic gold-toned loc beads for everyday glamour.", variants: Small (₦1,800), Medium (₦2,400), Large (₦3,000), is_featured: true
3. id: "prod_003", name: "Silver Beads", slug: "silver-beads", description: "Cool silver-toned loc beads — perfect for contemporary styles.", variants: Small (₦1,800), Medium (₦2,400), Large (₦3,000), is_featured: true
4. id: "prod_004", name: "Onyx Beads", slug: "onyx-beads", description: "Deep onyx loc beads for a bold, statement look.", variants: Small (₦2,000), Medium (₦2,600), Large (₦3,200), is_featured: true

All products: image "/images/products/placeholder-bead.jpg", in_stock: true for Small and Medium, false for Large. price_min and price_max derived from variants.

Variant IDs follow pattern: "var_001a", "var_001b", "var_001c" for product 001, etc.

**src/lib/mock/testimonials.ts** — Export Testimonial interface and TESTIMONIALS array:

Testimonial interface:
- id: string
- name: string  (e.g. "Adaeze O.")
- quote: string

TESTIMONIALS array — 3 items:
1. id: "t1", name: "Adaeze O.", quote: "These beads are absolutely stunning. I get compliments every single time I wear them."
2. id: "t2", name: "Funmi A.", quote: "Best quality loc accessories I have found in Nigeria. Fast delivery and beautiful packaging."
3. id: "t3", name: "Chiamaka B.", quote: "The 24K gold beads are exactly what I needed for my anniversary look. Worth every kobo."
  </action>
  <verify>Run: npx tsc --noEmit from the project root. No TypeScript errors on the new files.</verify>
  <done>src/lib/mock/products.ts and src/lib/mock/testimonials.ts exist, export their types and arrays, and TypeScript is satisfied (tsc --noEmit passes).</done>
</task>

<task type="auto">
  <name>Task 2: Add placeholder product image</name>
  <files>public/images/products/placeholder-bead.jpg</files>
  <action>
Create the directory public/images/products/ and add a placeholder image at public/images/products/placeholder-bead.jpg.

Generate a simple cream/gold branded placeholder. The recommended approach: create an SVG with brand colours (cream background #FAF3E0, gold text #C9A84C reading "Twinkle Locs") and save it as a JPEG or PNG.

Exact method — use Node.js to write a minimal valid JPEG placeholder, OR write a small SVG file at public/images/products/placeholder-bead.svg and reference that instead (next/image supports SVGs with explicit width/height). However, next/image fill mode works better with raster images.

Simplest working approach: Write a 400x400 SVG placeholder to public/images/products/placeholder-bead.jpg using this technique — create the file as an SVG with .jpg extension is not ideal. Instead:

1. Create public/images/products/ directory
2. Write a valid placeholder file. The simplest option for a pure-code environment: create a 1x1 pixel transparent PNG as a base64-decoded binary, OR write an SVG to public/images/products/placeholder-bead.svg and update the image path in products.ts to "/images/products/placeholder-bead.svg"

Use the SVG approach:
- Create public/images/products/placeholder-bead.svg
- SVG content: 400x400 viewBox, rect fill="#FAF3E0", centered text "Twinkle Locs" in gold (#C9A84C), font-size 32, font-family serif
- Update src/lib/mock/products.ts — change all image fields from "/images/products/placeholder-bead.jpg" to "/images/products/placeholder-bead.svg"

Then add the SVG extension to the images allowed list in next.config.ts:
- In next.config.ts, add `dangerouslyAllowSVG: true` under `images:` so next/image can serve the SVG. Also add `contentSecurityPolicy: "default-src 'self'; script-src 'none'; sandbox;"` as required by Next.js when dangerouslyAllowSVG is true.

If the SVG approach causes any friction, fall back to creating a minimal valid PNG file using a Node script: `node -e "require('fs').mkdirSync('public/images/products',{recursive:true}); require('fs').writeFileSync('public/images/products/placeholder-bead.png', Buffer.from('iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNk+M9QDwADhgGAWjR9awAAAABJRU5ErkJggg==','base64'))"` and update mock product image paths to "/images/products/placeholder-bead.png".

The only requirement: next/image does not 404 when loading the placeholder image, and the build passes.
  </action>
  <verify>
1. File exists at public/images/products/placeholder-bead.svg (or .png/.jpg depending on approach taken)
2. src/lib/mock/products.ts image paths match the actual filename
3. npx next build (or npm run dev + browser check) shows no Image 404 errors in the console
  </verify>
  <done>A placeholder image file exists in public/images/products/, all 4 mock products reference it with a valid path, and next/image can serve it without error.</done>
</task>

</tasks>

<verification>
- [ ] src/lib/mock/products.ts exists and exports MockProductVariant, MockProduct, FEATURED_PRODUCTS
- [ ] FEATURED_PRODUCTS has exactly 4 items matching: 24K Gold Beads, Gold Beads, Silver Beads, Onyx Beads
- [ ] Each product has 3 variants with realistic Naira prices
- [ ] price_min and price_max are correctly derived on each product
- [ ] src/lib/mock/testimonials.ts exists and exports Testimonial, TESTIMONIALS with 3 items
- [ ] A placeholder image file exists under public/images/products/
- [ ] All mock product image paths reference the actual placeholder filename
- [ ] npx tsc --noEmit passes with no errors on the new files
</verification>

<success_criteria>
Mock data layer is complete: any component can import FEATURED_PRODUCTS or TESTIMONIALS and get correctly typed data. The placeholder image renders without 404. TypeScript is clean.
</success_criteria>

<output>
After completion, create .planning/phases/02-homepage/02-01-SUMMARY.md
</output>
