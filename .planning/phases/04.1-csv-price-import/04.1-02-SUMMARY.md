---
phase: 04.1-csv-price-import
plan: 02
subsystem: ui
tags: [react, nextjs, tailwind, product-detail, price-tiers]

# Dependency graph
requires:
  - phase: 04.1-csv-price-import/01
    provides: PriceTier type on ProductVariant, mock data with real tier arrays
provides:
  - Pack-size tier picker in ProductDetailClient replacing integer quantity stepper
  - selectedTierQty state exposed for Phase 5 cart wiring
  - Price display driven by both selectedVariantId + selectedTierQty
  - Shears graceful degradation (no picker rendered for single-tier variants)
affects: [05-cart, phase5-cart-context]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Tier-driven price display: selectedTier?.price ?? selectedVariant?.price ?? product.price_min"
    - "Variant-change resets tier: handleVariantChange sets selectedTierQty to new variant's first tier"
    - "Conditional picker: only render section when price_tiers.length > 1"

key-files:
  created: []
  modified:
    - src/components/product/ProductDetailClient.tsx

key-decisions:
  - "selectedTierQty initialised from first tier of first in-stock variant at mount"
  - "handleVariantChange resets selectedTierQty to new variant's first tier to prevent stale cross-variant tier"
  - "Pack label 'Pack of N beads' hidden (not just invisible) for single-tier products via conditional render"
  - "Shears degrades gracefully: price_tiers.length <= 1 suppresses picker and pack label"
  - "displayPrice fallback chain: selectedTier?.price ?? selectedVariant?.price ?? product.price_min"

patterns-established:
  - "selectedVariantId + selectedTierQty: both tracked in state; Phase 5 reads both for cart wiring"
  - "Pack-size button styling mirrors size-picker button styling (border-gold/bg-gold/10 selected, border-charcoal/20 unselected)"

# Metrics
duration: 5min
completed: 2026-03-23
---

# Phase 4.1 Plan 02: Pack-Size Tier Picker Summary

**Pack-size tier picker replaces integer quantity stepper in ProductDetailClient — price updates live on both variant and tier selection; selectedTierQty state wired and ready for Phase 5 cart context**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-03-23T00:00:00Z
- **Completed:** 2026-03-23T00:05:00Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments

- Removed +/- integer quantity stepper and replaced with tier buttons (e.g. "50 beads", "100 beads")
- Price display now reflects the selected pack-size tier, not just the variant's base price
- Shears product (single-tier) shows no picker and no "Pack of N beads" label — degrades gracefully
- `selectedTierQty` state exposed alongside `selectedVariantId` for Phase 5 cart wiring

## Task Commits

Each task was committed atomically:

1. **Task 1: Replace quantity stepper with pack-size tier picker** - `f06dbe1` (feat)

**Plan metadata:** (see docs commit below)

## Files Created/Modified

- `src/components/product/ProductDetailClient.tsx` — Replaced quantity state + stepper with selectedTierQty + tier picker; added displayPrice derivation; Shears edge case handled

## Decisions Made

- `handleVariantChange` encapsulates both `setSelectedVariantId` + `setSelectedTierQty` reset in a single function — prevents stale tier (e.g. switching from 6mm with 25-bead first tier to 2mm whose tiers start at 50) causing a tier mismatch where selectedTierQty has no matching entry in the new variant's price_tiers
- Pack label below price uses conditional render (not CSS visibility) so it takes no vertical space on Shears page
- TODO comment updated to `/* TODO Phase 5: wire selectedVariantId + selectedTierQty to cart context */` to accurately describe Phase 5 contract

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- `selectedVariantId` and `selectedTierQty` are both tracked in state and ready for Phase 5 cart context wiring
- Price display is fully reactive to both selections
- Shears edge case handled; no special-casing needed in Phase 5

---
*Phase: 04.1-csv-price-import*
*Completed: 2026-03-23*
