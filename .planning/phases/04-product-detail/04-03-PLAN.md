---
phase: 04-product-detail
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - src/components/product/ProductDetailClient.tsx
  - src/app/catalog/[slug]/page.tsx
autonomous: true

must_haves:
  truths:
    - "Visiting /catalog/24k-gold-beads shows an inline size picker with Small, Medium, Large variant buttons"
    - "Clicking an in-stock variant button highlights it (gold border/bg) and updates the displayed price"
    - "Large (8mm) variant button is visually disabled (dimmed, cursor-not-allowed) and cannot be selected"
    - "A quantity stepper (minus/number/plus) allows selecting 1–10 beads — defaults to 1"
    - "5 static thread colour swatches are displayed with a label — they are decorative only (no selection state affects price)"
    - "An 'Add to Cart' button is visible and enabled — clicking it does nothing in Phase 4 (no-op with TODO comment)"
    - "On first render, the first in-stock variant is pre-selected (not the out-of-stock Large)"
  artifacts:
    - path: "src/components/product/ProductDetailClient.tsx"
      provides: "Client island: variant picker, quantity stepper, thread colour swatches, Add to Cart button"
      exports: ["ProductDetailClient"]
    - path: "src/app/catalog/[slug]/page.tsx"
      provides: "Updated page.tsx passing product to ProductDetailClient, replacing the Plan 04-02 placeholder"
      contains: "ProductDetailClient"
  key_links:
    - from: "src/app/catalog/[slug]/page.tsx"
      to: "ProductDetailClient"
      via: "product prop (typed Product)"
      pattern: "<ProductDetailClient product=\\{product\\}"
    - from: "ProductDetailClient"
      to: "product.variants"
      via: "selectedVariantId state drives price display; in_stock drives disabled state"
      pattern: "selectedVariantId.*variant\\.price"
    - from: "ProductDetailClient"
      to: "ProductImageGallery"
      via: "gallery rendered inside ProductDetailClient (client island wraps gallery)"
      pattern: "<ProductImageGallery"
---

<objective>
Build the ProductDetailClient island — the interactive variant picker, quantity stepper, thread colour placeholder, and Add to Cart button. Wire it into page.tsx replacing the Plan 04-02 placeholder.

Purpose: This is the core interaction layer for the product detail page. The logic is adapted from AddToCartModal (which already has the variant selection pattern with out-of-stock handling) but rendered inline instead of in a modal.
Output: Fully interactive variant picker wired into the detail page. Price updates on variant selection. Out-of-stock variants disabled. Add to Cart is a Phase 5 no-op.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-product-detail/04-RESEARCH.md
@src/lib/types/product.ts
@src/components/home/AddToCartModal.tsx
@src/app/catalog/[slug]/page.tsx
@src/components/product/ProductImageGallery.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ProductDetailClient island</name>
  <files>src/components/product/ProductDetailClient.tsx</files>
  <action>
    Create `src/components/product/ProductDetailClient.tsx` as a `'use client'` component.

    Props: `{ product: Product }` — import Product from `@/lib/types/product`.

    State:
    - `selectedVariantId`: initialised to `product.variants.find(v => v.in_stock)?.id ?? product.variants[0]?.id ?? ''`
    - `quantity`: `useState(1)`

    Derived: `const selectedVariant = product.variants.find(v => v.id === selectedVariantId)`

    Render structure — a single `<div>` (not a full-page layout; page.tsx owns the grid):

    **Material badge:** small pill `<span>` showing `product.material`. Same colour logic as page.tsx:
    - Gold → `bg-gold/20 text-cocoa`; Silver → `bg-stone text-charcoal`; Crystal → `bg-cream border border-charcoal/20 text-charcoal`; Tools → `bg-forest/20 text-forest`

    **Product name h1:** `font-display text-3xl md:text-4xl text-cocoa mb-2`

    **Price:** `font-heading text-2xl text-gold mb-6` — shows `₦{(selectedVariant?.price ?? product.price_min).toLocaleString()}`

    **Description:** `font-body text-charcoal/80 leading-relaxed mb-8`

    **Size picker section:**
    Label: `<p className="font-heading text-sm font-semibold text-cocoa mb-3 uppercase tracking-wide">Size</p>`
    Variant buttons — copy the exact button pattern from `AddToCartModal.tsx` (look at that file for the selected/unselected/disabled class pattern):
    - In-stock + selected: `border-gold bg-gold/10 text-cocoa font-semibold`
    - In-stock + unselected: `border-charcoal/20 text-charcoal hover:border-gold`
    - Out-of-stock: `opacity-40 cursor-not-allowed border-charcoal/10 text-charcoal/40` — button is `disabled`
    - Each button shows variant.name and, when out-of-stock, a `<span className="block text-xs font-normal">(Out of stock)</span>` below it

    **Quantity stepper section:**
    Label: `<p className="font-heading text-sm font-semibold text-cocoa mb-3 uppercase tracking-wide">Quantity</p>`
    Row: `[−] [number display] [+]` buttons
    - Minus button: calls `setQuantity(q => Math.max(1, q - 1))`; visually disabled (opacity-40) when quantity === 1
    - Plus button: calls `setQuantity(q => Math.min(10, q + 1))`; visually disabled when quantity === 10
    - Number display: `<span className="w-12 text-center font-body text-lg">{quantity}</span>`

    **Thread colour section (static placeholder):**
    Label: `<p className="font-heading text-sm font-semibold text-cocoa mb-3 uppercase tracking-wide">Thread Colour</p>`
    5 hardcoded colour swatches as small circles (32px × 32px) in a row — use these exact colours:
    - Black: `bg-[#1A1A1A]`
    - Gold: `bg-gold`
    - Red: `bg-terracotta`
    - White: `bg-cream border border-charcoal/20`
    - Forest: `bg-forest`
    Below the swatches: `<p className="text-xs text-charcoal/50 mt-2 font-body">Colour selection coming soon</p>`
    These swatches are decorative only — no onClick, no state. Add a `// TODO Phase 5: wire thread colour to cart line item` comment.

    **Add to Cart button:**
    `<button onClick={() => { /* TODO Phase 5: wire to cart context */ }} className="w-full bg-gold text-cocoa font-heading font-semibold py-4 rounded-lg hover:bg-terracotta hover:text-cream transition-colors mt-8">`
    Text: "Add to Cart"
    This button is always enabled (never disabled in Phase 4).

    Also render `<ProductImageGallery images={product.images?.length ? product.images : [product.image]} alt={product.name} />` — this component lives INSIDE ProductDetailClient because it requires client state (selected image index). Import from `./ProductImageGallery`.

    Note: ProductDetailClient renders the gallery + the right-column product info together. page.tsx will pass the full product object and render the 2-column grid around ProductDetailClient.
  </action>
  <verify>
    1. `npx tsc --noEmit` → zero errors
    2. Component exports `ProductDetailClient`
    3. No Supabase import anywhere in this file (data comes via props — rule: Supabase only in Server Components)
  </verify>
  <done>ProductDetailClient.tsx exists with all sections: material badge, name, price, size picker, quantity stepper, thread colour swatches, Add to Cart. Types compile cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ProductDetailClient into page.tsx</name>
  <files>src/app/catalog/[slug]/page.tsx</files>
  <action>
    Update src/app/catalog/[slug]/page.tsx to replace the placeholder product info section from Plan 04-02 with `<ProductDetailClient product={product} />`.

    The page.tsx grid now becomes:
    ```
    <div className="max-w-6xl mx-auto px-4 py-12 grid grid-cols-1 md:grid-cols-2 gap-12">
      {/* Left: Gallery — now rendered inside ProductDetailClient */}
      {/* Right: ProductDetailClient owns the full right column */}
      <ProductDetailClient product={product} />
    </div>
    ```

    Wait — ProductDetailClient renders the gallery too (it wraps gallery + product info together in a `<div className="contents">`). So the page.tsx grid receives TWO children: the gallery output and the product info output from the client island.

    Actually the cleaner approach: ProductDetailClient renders a `<>` fragment with two children that both slot into the grid:
    - First child (grid col 1): `<ProductImageGallery ... />`
    - Second child (grid col 2): the product info div

    This way page.tsx does:
    ```tsx
    <div className="max-w-6xl mx-auto px-4 py-12 grid grid-cols-1 md:grid-cols-2 gap-12">
      <ProductDetailClient product={product} />
    </div>
    ```
    And ProductDetailClient returns a fragment `<>` with gallery as first element and info div as second element — CSS grid will place them in two columns automatically.

    Also remove the separate `<ProductImageGallery>` import from page.tsx since it is now internal to ProductDetailClient.

    Remove the Plan 04-02 placeholder text ("Variant picker coming in next plan").

    The breadcrumb nav block above the grid stays in page.tsx (it's a Server Component concern — no interactivity).

    Page.tsx still handles: awaiting params, Supabase fetch, notFound() guard, cast to Product type, breadcrumb. It no longer renders gallery or product info directly — those come from ProductDetailClient.
  </action>
  <verify>
    1. `npx tsc --noEmit` → zero errors
    2. `npm run build` → clean build
    3. Visit http://localhost:3000/catalog/24k-gold-beads: see breadcrumb, gallery (placeholder SVG), product name "24K Gold Beads", gold price, description, size buttons (Small/Medium/Large), quantity stepper, thread colour swatches, Add to Cart button
    4. Click Medium (6mm) → price updates to ₦3,200
    5. Large (8mm) button is visually dimmed and cannot be clicked
    6. Quantity − below 1 is blocked; + above 10 is blocked
  </verify>
  <done>page.tsx passes product to ProductDetailClient; full interactive variant picker renders on product detail page; no TypeScript errors; build clean.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` → zero errors
- `npm run build` → clean
- /catalog/24k-gold-beads: size picker visible, price updates on variant click, Large disabled, quantity stepper works 1–10, thread colour swatches visible (decorative), Add to Cart button present
- /catalog/shears: single "Standard" variant button shown (in-stock), no out-of-stock state, single fixed price
</verification>

<success_criteria>
Interactive variant picker renders inline on the product detail page. Size selection updates price. Out-of-stock variants are disabled. Quantity stepper works. Thread colour swatches are decorative placeholder. Add to Cart is a Phase 5 no-op.
</success_criteria>

<output>
After completion, create `.planning/phases/04-product-detail/04-03-SUMMARY.md`
</output>
