---
phase: 04-product-detail
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/catalog/[slug]/page.tsx
  - src/app/catalog/[slug]/not-found.tsx
  - src/components/product/ProductImageGallery.tsx
autonomous: true

must_haves:
  truths:
    - "Visiting /catalog/24k-gold-beads renders the full product page (not the stub placeholder)"
    - "Product name, description, material badge, price range, and breadcrumb are visible on the page"
    - "Image gallery renders the placeholder SVG as the main image"
    - "Visiting /catalog/nonexistent-slug renders a branded 404 page with a link back to /catalog"
    - "Thumbnail rail is hidden when only one image exists (all current products have one image)"
    - "The page fetches product data from Supabase — no mock import"
  artifacts:
    - path: "src/app/catalog/[slug]/page.tsx"
      provides: "Async Server Component fetching product by slug, rendering layout with gallery + info"
      contains: "notFound()"
    - path: "src/app/catalog/[slug]/not-found.tsx"
      provides: "Branded 404 page for invalid product slugs"
      contains: "Back to catalog"
    - path: "src/components/product/ProductImageGallery.tsx"
      provides: "Client component rendering main image + conditional thumbnail rail"
      exports: ["ProductImageGallery"]
  key_links:
    - from: "src/app/catalog/[slug]/page.tsx"
      to: "supabase.from('products').eq('slug', slug).single()"
      via: "createClient from @/lib/supabase/server"
      pattern: "from\\('products'\\).*eq\\('slug'"
    - from: "src/app/catalog/[slug]/page.tsx"
      to: "ProductImageGallery"
      via: "images prop: product.images?.length ? product.images : [product.image]"
      pattern: "product\\.images"
    - from: "ProductImageGallery"
      to: "next/image"
      via: "fill + sizes prop — same pattern as CatalogProductCard"
      pattern: "fill.*sizes"
---

<objective>
Replace the /catalog/[slug] stub with a full Server Component layout. Build the ProductImageGallery client component. Add a branded not-found page for invalid slugs.

Purpose: This plan establishes the page skeleton and image display. It is pure server-side rendering and static layout — no variant state yet. Subsequent plans layer client interactivity on top.
Output: Fully rendered product detail page with breadcrumb, gallery, product name/description/price/material, and a 404 page for invalid slugs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-product-detail/04-RESEARCH.md
@src/lib/types/product.ts
@src/lib/types/review.ts
@src/lib/supabase/server.ts
@src/types/supabase.ts
@src/app/catalog/[slug]/page.tsx
@src/components/catalog/CatalogProductCard.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ProductImageGallery client component</name>
  <files>src/components/product/ProductImageGallery.tsx</files>
  <action>
    Create the directory `src/components/product/` if it does not exist. Create `ProductImageGallery.tsx` as a `'use client'` component.

    Props: `{ images: string[], alt: string }`

    Behaviour:
    - Track `selectedIndex` with `useState(0)`
    - Main image area: full-width container with `aspect-square relative rounded-xl overflow-hidden bg-stone` — render `<Image fill sizes="(max-width: 768px) 100vw, 50vw" src={images[selectedIndex]} alt={alt} className="object-contain" />` (use object-contain not object-cover because current images are SVGs with transparent backgrounds)
    - Thumbnail rail: only rendered when `images.length > 1`. Map over images as small clickable buttons (`64px × 64px`). Active thumbnail: `ring-2 ring-gold`. Inactive: `opacity-60 hover:opacity-100`. Clicking sets `selectedIndex`.
    - When `images.length <= 1`: render ONLY the main image area, no thumbnail rail.

    Use `next/image` (already configured with `dangerouslyAllowSVG: true` in next.config.ts).
    Use Tailwind v4 tokens: `bg-stone`, `ring-gold`, `rounded-xl`. No bg-gradient — use `bg-linear-to-br` if any gradient is needed.
  </action>
  <verify>`npx tsc --noEmit` passes. Component exports `ProductImageGallery`. The thumbnail rail logic is conditional on `images.length > 1`.</verify>
  <done>ProductImageGallery.tsx exists, compiles, exports the named component, and handles the single-image case by suppressing the thumbnail rail.</done>
</task>

<task type="auto">
  <name>Task 2: Replace stub with full product detail page + not-found</name>
  <files>src/app/catalog/[slug]/page.tsx, src/app/catalog/[slug]/not-found.tsx</files>
  <action>
    Replace src/app/catalog/[slug]/page.tsx entirely. It is an async Server Component (no 'use client'). Pattern follows the catalog page.tsx for Supabase query setup.

    Structure:
    1. Import: `notFound` from `next/navigation`, `createClient` from `@/lib/supabase/server`, Product/ProductVariant from `@/lib/types/product`, `ProductImageGallery` from `@/components/product/ProductImageGallery`
    2. Await params (Next.js 15 pattern — already in the stub, keep it): `const { slug } = await params`
    3. Fetch product: `supabase.from('products').select('*').eq('slug', slug).single()`
    4. If no product: call `notFound()`
    5. Cast raw row to Product: `const product: Product = { ...rawProduct, variants: rawProduct.variants as unknown as ProductVariant[], images: rawProduct.images?.length ? rawProduct.images : undefined }`
    6. Derive `galleryImages`: `product.images ?? [product.image]`

    Page layout (no client islands yet — ProductDetailClient comes in Plan 04-03):
    ```
    <main className="bg-cream min-h-screen">
      {/* Breadcrumb */}
      <nav> Catalog → {product.name} (Link to /catalog, then span for product name) </nav>

      {/* Main content grid — 2-col on md+, stacked on mobile */}
      <div className="max-w-6xl mx-auto px-4 py-12 grid grid-cols-1 md:grid-cols-2 gap-12">
        {/* Left: Gallery */}
        <ProductImageGallery images={galleryImages} alt={product.name} />

        {/* Right: Product info — placeholder for ProductDetailClient */}
        <div>
          <span className="material badge" /> {/* small pill: product.material */}
          <h1 className="font-display text-3xl md:text-4xl text-cocoa mb-2">{product.name}</h1>
          <p className="font-heading text-2xl text-gold mb-4">
            {product.price_min === product.price_max
              ? `₦${product.price_min.toLocaleString()}`
              : `₦${product.price_min.toLocaleString()} – ₦${product.price_max.toLocaleString()}`}
          </p>
          <p className="font-body text-charcoal/80 leading-relaxed mb-8">{product.description}</p>
          {/* TODO Plan 04-03: replace this section with <ProductDetailClient product={product} /> */}
          <p className="font-body text-charcoal/50 text-sm italic">Variant picker coming in next plan</p>
        </div>
      </div>
    </main>
    ```

    Material badge colours:
    - Gold: `bg-gold/20 text-cocoa`
    - Silver: `bg-stone text-charcoal`
    - Crystal: `bg-cream border border-charcoal/20 text-charcoal`
    - Tools: `bg-forest/20 text-forest`

    Breadcrumb: `<nav className="max-w-6xl mx-auto px-4 pt-6 pb-0">` — `<Link href="/catalog" className="text-gold hover:underline font-body text-sm">Catalog</Link> <span className="text-charcoal/40 mx-1">›</span> <span className="font-body text-sm text-charcoal/70">{product.name}</span>`

    Create src/app/catalog/[slug]/not-found.tsx:
    - Simple centered layout on `bg-cream min-h-screen`
    - Heading: "Product not found" in `font-display text-cocoa`
    - Subtext: "This product doesn't exist or may have been removed."
    - Link back to `/catalog` styled like the breadcrumb link
  </action>
  <verify>
    1. `npx tsc --noEmit` → zero errors
    2. `npm run build` → no build errors
    3. Visit http://localhost:3000/catalog/24k-gold-beads (with Supabase connected) — page renders with product name, description, material badge, price range, and the placeholder image
    4. Visit http://localhost:3000/catalog/nonexistent — renders the not-found page with "Product not found" heading
  </verify>
  <done>Product detail page renders real data from Supabase with breadcrumb, gallery, product info section. Invalid slugs render a branded not-found page. No TypeScript errors.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` → zero errors
- `npm run build` → clean build
- /catalog/24k-gold-beads renders: breadcrumb, placeholder image, "24K Gold Beads" h1, gold price, description text, material badge
- /catalog/shears renders: "Tools" material badge (forest colour)
- /catalog/nonexistent triggers not-found.tsx
- Thumbnail rail is absent (all products have single image currently)
</verification>

<success_criteria>
Full product detail page layout renders from Supabase data. Gallery, breadcrumb, product info all visible. Invalid slugs handled gracefully with branded 404 page.
</success_criteria>

<output>
After completion, create `.planning/phases/04-product-detail/04-02-SUMMARY.md`
</output>
