---
phase: 07-content-pages
plan: "04"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/app/(admin)/admin/shipping/page.tsx
  - src/app/(admin)/_components/ShippingForm.tsx
  - src/app/api/admin/shipping/route.ts
  - src/app/shipping/page.tsx
autonomous: true

must_haves:
  truths:
    - "Visitor can navigate to /shipping and see Lagos ₦3,000 / Other states ₦4,500 rates with timeframes"
    - "International shipping section shows a WhatsApp CTA button with pre-filled message"
    - "Admin can update rates, timeframes, and intro copy from /admin/shipping"
    - "Shipping content is stored in the existing settings table under shipping_* keys"
  artifacts:
    - path: "src/app/shipping/page.tsx"
      provides: "Server Component — reads shipping settings from Supabase, renders public page"
    - path: "src/app/(admin)/admin/shipping/page.tsx"
      provides: "Admin Server Component — fetches shipping settings, passes to ShippingForm"
    - path: "src/app/(admin)/_components/ShippingForm.tsx"
      provides: "use client form — edits shipping_* settings keys"
    - path: "src/app/api/admin/shipping/route.ts"
      provides: "PUT handler — upserts shipping settings rows"
  key_links:
    - from: "src/app/shipping/page.tsx"
      to: "settings Supabase table"
      via: "createClient().from('settings').select('*').in('key', shippingKeys)"
      pattern: "settings.*shipping"
    - from: "src/app/(admin)/_components/ShippingForm.tsx"
      to: "/api/admin/shipping"
      via: "fetch PUT with shipping settings payload"
      pattern: "api/admin/shipping"
---

<objective>
Build the /shipping information page and its admin editor. Shipping content lives in the existing settings table (no new DB table needed) — the admin editor is a focused version of SettingsForm scoped to shipping keys.

Purpose: Satisfies CONT-04 — Shipping Info page details domestic rates/timeframes and explains the international inquiry process.
Output: Public /shipping page with zone rates + WhatsApp CTA; /admin/shipping editor for copy and rates.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-content-pages/07-CONTEXT.md
@.planning/phases/07-content-pages/07-RESEARCH.md
@.planning/phases/07-content-pages/07-01-SUMMARY.md
@src/types/supabase.ts
@src/lib/supabase/server.ts
@src/lib/supabase/admin.ts
@src/app/(admin)/admin/settings/page.tsx
@src/app/(admin)/_components/SettingsForm.tsx
@src/app/api/admin/settings/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shipping admin editor and API route</name>
  <files>
    src/app/(admin)/admin/shipping/page.tsx
    src/app/(admin)/_components/ShippingForm.tsx
    src/app/api/admin/shipping/route.ts
  </files>
  <action>
    **Settings keys used (all strings in the settings table):**
    ```
    shipping_lagos_rate         "3000"
    shipping_other_rate         "4500"
    shipping_lagos_days         "1–2 business days"
    shipping_other_days         "3–5 business days"
    shipping_intl_message       "Hi, I'd like a shipping quote for an international order."
    shipping_page_intro         "We deliver across Nigeria..."
    ```

    **src/app/api/admin/shipping/route.ts**
    PUT only. Auth guard: createClient() + getUser(). Accept body: `Record<string, string>` — an object of shipping key/value pairs. Filter to only the six known shipping keys (allowlist — never write arbitrary keys). Use createAdminClient() to upsert all rows into settings with `{ onConflict: 'key' }` (same pattern as /api/admin/settings). Return `{ ok: true }` on success, 401 on missing auth, 500 on DB error.

    Read the existing /api/admin/settings/route.ts to understand the exact upsert pattern and copy it directly — the logic is identical, just scoped to shipping keys.

    **src/app/(admin)/admin/shipping/page.tsx**
    Server Component. Auth check + redirect. Fetch only shipping keys: `createAdminClient().from('settings').select('*').in('key', ['shipping_lagos_rate', 'shipping_other_rate', 'shipping_lagos_days', 'shipping_other_days', 'shipping_intl_message', 'shipping_page_intro'])`. Build a `Record<string, string>` from the result rows. Pass to `<ShippingForm settings={settings} />`.

    **src/app/(admin)/_components/ShippingForm.tsx**
    `'use client'`. Props: `{ settings: Record<string, string> }`. State: one controlled input per key, initialised from props with fallback defaults (e.g. shipping_lagos_rate defaults to '3000').

    Render as labeled form sections:
    - **Domestic Delivery** section: Lagos rate (₦ input), Lagos timeframe (text), Other states rate (₦ input), Other states timeframe (text)
    - **International Shipping** section: pre-filled WhatsApp message text (textarea — this is the text that will be URL-encoded into the WhatsApp link)
    - **Page intro copy** section: textarea for the intro paragraph

    Save button: PUT to /api/admin/shipping with `{ shipping_lagos_rate, shipping_other_rate, ... }`. Toast pattern: same as SettingsForm (useState + setTimeout(3000)).

    Do NOT use Tiptap here — plain text inputs/textareas only (as specified in CONTEXT.md for the shipping page).
  </action>
  <verify>
    1. `npx tsc --noEmit` exits 0.
    2. /admin/shipping loads showing all six editable fields with sensible defaults.
    3. Change a rate and save — toast shows "Saved"; no console errors.
  </verify>
  <done>Admin can update shipping rates, timeframes, and international message copy from /admin/shipping; data saved to settings table; no TypeScript errors.</done>
</task>

<task type="auto">
  <name>Task 2: Public /shipping page</name>
  <files>
    src/app/shipping/page.tsx
  </files>
  <action>
    Server Component (no auth required). Use `createClient()`. Fetch shipping keys: `.from('settings').select('key, value').in('key', ['shipping_lagos_rate', 'shipping_other_rate', 'shipping_lagos_days', 'shipping_other_days', 'shipping_intl_message', 'shipping_page_intro'])`. Build a settings object with fallback defaults for all keys (in case settings table is empty on first load):
    ```typescript
    const defaults = {
      shipping_lagos_rate: '3000',
      shipping_other_rate: '4500',
      shipping_lagos_days: '1–2 business days',
      shipping_other_days: '3–5 business days',
      shipping_intl_message: "Hi, I'd like a shipping quote for an international order.",
      shipping_page_intro: 'We deliver across Nigeria.',
    }
    ```

    Page layout — choose a clean two-zone presentation:
    - Page heading: "Shipping Information"
    - Intro paragraph from `shipping_page_intro`
    - **Domestic Delivery** section: two-column card grid (or a simple table) showing:
      - Lagos: ₦{shipping_lagos_rate.toLocaleString('en-NG')} — {shipping_lagos_days}
      - All other states: ₦{shipping_other_rate.toLocaleString('en-NG')} — {shipping_other_days}
    - **International Orders** section: explanatory paragraph ("We offer international shipping on request") + a WhatsApp CTA `<a>` tag:
      ```typescript
      const waUrl = `https://wa.me/2348000000000?text=${encodeURIComponent(settings.shipping_intl_message)}`
      ```
      Button text: "Request a Shipping Quote" — same WhatsApp button style used elsewhere on the site (gold or brand-green background). Use `<a href={waUrl} target="_blank" rel="noopener noreferrer">`.

    Style guidelines: use the afro-luxury brand aesthetic — `font-heading` for headings, `text-charcoal` for body, gold accents on CTAs. Domestic zone cards/table should feel clear and premium — a two-column comparison card layout works well here.

    Max width `max-w-3xl mx-auto px-4 py-12` wrapper.

    This is a pure Server Component — no `'use client'` needed. The WhatsApp `<a>` tag is a plain anchor (no client state required).
  </action>
  <verify>
    1. `npx tsc --noEmit` exits 0.
    2. Visit /shipping — Lagos and other-states rates + timeframes visible.
    3. International section shows WhatsApp CTA button; href includes pre-filled message text.
    4. Page renders even when settings table has no shipping keys (fallback defaults used).
  </verify>
  <done>Public /shipping page shows domestic rates, timeframes, and a WhatsApp international quote CTA; renders correctly with fallback defaults; no TypeScript errors.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes with zero errors.
2. /shipping renders Lagos ₦3,000 / Other states ₦4,500 with timeframes.
3. International WhatsApp CTA button present with pre-filled message.
4. /admin/shipping editor saves correctly to settings table.
5. No new DB table created — uses existing settings table.
</verification>

<success_criteria>
- CONT-04: Shipping page shows domestic rates, timeframes, and international inquiry process
- Admin can update content without code changes
- WhatsApp CTA uses pre-filled message from settings
- Build passes with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-content-pages/07-04-SUMMARY.md`
</output>
