---
phase: 07-content-pages
plan: "05"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/app/(admin)/admin/blog/page.tsx
  - src/app/(admin)/admin/blog/new/page.tsx
  - src/app/(admin)/admin/blog/[id]/page.tsx
  - src/app/(admin)/_components/BlogPostForm.tsx
  - src/app/api/admin/blog/route.ts
  - src/app/api/admin/blog/[id]/route.ts
autonomous: true

must_haves:
  truths:
    - "Admin can create a new blog post with title, slug, excerpt, body (Tiptap), featured image, tag, and published toggle"
    - "Admin can edit and delete existing blog posts from /admin/blog/[id]"
    - "Slug auto-generates from title on blur when slug field is empty (create mode only)"
    - "Published toggle controls whether the post appears on the public blog"
    - "Blog posts with featured images upload to Supabase Storage content-images bucket"
  artifacts:
    - path: "src/app/(admin)/admin/blog/page.tsx"
      provides: "Admin blog post list page"
    - path: "src/app/(admin)/admin/blog/new/page.tsx"
      provides: "Create new blog post page"
    - path: "src/app/(admin)/admin/blog/[id]/page.tsx"
      provides: "Edit blog post page"
    - path: "src/app/(admin)/_components/BlogPostForm.tsx"
      provides: "use client form — create and edit blog posts with Tiptap + image upload"
    - path: "src/app/api/admin/blog/route.ts"
      provides: "POST handler — creates new blog_posts row"
    - path: "src/app/api/admin/blog/[id]/route.ts"
      provides: "PUT and DELETE handlers for individual blog post"
  key_links:
    - from: "src/app/(admin)/_components/BlogPostForm.tsx"
      to: "/api/admin/blog"
      via: "fetch POST (create) or fetch PUT /api/admin/blog/[id] (edit)"
      pattern: "api/admin/blog"
    - from: "src/app/(admin)/_components/BlogPostForm.tsx"
      to: "content-images Supabase Storage bucket"
      via: "createBrowserClient().storage.from('content-images').upload()"
      pattern: "content-images"
---

<objective>
Build the /admin/blog CRUD interface so Unoma can create and manage blog posts. This plan covers only the admin side — the public-facing /blog and /blog/[slug] pages are Plan 06 (Wave 3). Plan 05 must complete first because Plan 06 depends on the blog_posts data layer being established.

Purpose: Enables blog post creation which Plan 06 reads from Supabase.
Output: /admin/blog list, /admin/blog/new, /admin/blog/[id] edit — full CRUD for blog posts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-content-pages/07-CONTEXT.md
@.planning/phases/07-content-pages/07-RESEARCH.md
@.planning/phases/07-content-pages/07-01-SUMMARY.md
@src/types/supabase.ts
@src/lib/supabase/server.ts
@src/lib/supabase/admin.ts
@src/app/(admin)/admin/products/page.tsx
@src/app/(admin)/admin/products/new/page.tsx
@src/app/(admin)/admin/products/[id]/page.tsx
@src/app/(admin)/_components/ProductForm.tsx
@src/app/api/admin/products/route.ts
@src/app/api/admin/products/[id]/route.ts
@src/app/(admin)/_components/RichTextEditor.tsx
@src/app/(admin)/_components/ImageUploader.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Blog API routes (POST + PUT/DELETE)</name>
  <files>
    src/app/api/admin/blog/route.ts
    src/app/api/admin/blog/[id]/route.ts
  </files>
  <action>
    **src/app/api/admin/blog/route.ts**
    POST only. Auth guard: createClient() + getUser(). Body: `{ title: string; slug: string; body: string; excerpt: string; featured_image: string | null; tag: string | null; published: boolean }`. Validate: title and slug required. If published=true and published_at is not provided, set published_at to `new Date().toISOString()`. Use createAdminClient().from('blog_posts').insert(row).select().single(). Return `{ post }` on success (201), 401 on missing auth, 400 on validation error, 409 on slug conflict (unique constraint violation), 500 on other DB error.

    **src/app/api/admin/blog/[id]/route.ts**
    PUT and DELETE. Both require auth guard. PUT body: same shape as POST body but all fields optional (Partial). If `published` transitions from false to true and body.published_at is not provided, set published_at to now. Use createAdminClient().from('blog_posts').update(updates).eq('id', id).select().single(). DELETE: createAdminClient().from('blog_posts').delete().eq('id', id). Both return `{ ok: true }` on success, 401, 404, 500 appropriately.

    Follow the exact same pattern as /api/admin/products/[id]/route.ts — read that file for the auth guard + error handling pattern.
  </action>
  <verify>
    `npx tsc --noEmit` exits 0. Route files compile without errors.
  </verify>
  <done>Blog API routes handle POST/PUT/DELETE with auth guard; slug conflict returns 409; published_at auto-set when publishing; TypeScript clean.</done>
</task>

<task type="auto">
  <name>Task 2: Blog admin pages and BlogPostForm component</name>
  <files>
    src/app/(admin)/admin/blog/page.tsx
    src/app/(admin)/admin/blog/new/page.tsx
    src/app/(admin)/admin/blog/[id]/page.tsx
    src/app/(admin)/_components/BlogPostForm.tsx
  </files>
  <action>
    **src/app/(admin)/_components/BlogPostForm.tsx**
    `'use client'`. Props: `{ post?: BlogPost }`. Mode detection: `const isEdit = !!post`.

    State: title, slug, excerpt, body, featured_image, tag, published (boolean, default false).

    Fields:
    - **Title**: text input. On blur (when slug is empty AND not isEdit): auto-generate slug via `generateSlug(title)`. Copy the `generateSlug` function from ProductForm (kebab-case, lowercase, strip special chars).
    - **Slug**: text input (editable; auto-filled from title blur in create mode only — do not overwrite existing slug in edit mode)
    - **Excerpt**: textarea (plain text, 1-3 sentences — displayed on blog listing cards)
    - **Tag**: text input (freeform — e.g. "Hair Care", "Tutorials", "Brand Story")
    - **Featured image**: single image upload using a simplified uploader — createBrowserClient() + storage.from('content-images') + path `blog/${Date.now()}-${filename}`. Show current image as thumbnail preview. Show a "Remove image" button that clears featured_image to null.
    - **Body**: `<RichTextEditor value={body} onChange={setBody} />` — same Tiptap editor used for product descriptions. immediatelyRender: false is already set inside RichTextEditor.
    - **Published**: checkbox toggle with label "Publish post (visible on blog)"

    Save button: POST (create) or PUT (edit) to appropriate API route. Toast on success/error. On successful create: `router.push('/admin/blog')`.

    Delete button (edit mode only): inline confirm pattern — same as ProductForm. On confirm: DELETE to /api/admin/blog/[id], then `router.push('/admin/blog')`.

    **src/app/(admin)/admin/blog/page.tsx**
    Server Component. Auth check. Fetch: `createAdminClient().from('blog_posts').select('id, title, slug, tag, published, published_at, created_at').order('created_at', { ascending: false })`. Render:
    - Heading "Blog Posts"
    - "New Post" button linking to /admin/blog/new
    - Table of posts: title, tag, published badge (green "Published" / grey "Draft"), published_at date, Edit link

    **src/app/(admin)/admin/blog/new/page.tsx**
    Server Component. Auth check. Render `<BlogPostForm />` (no post prop — create mode).

    **src/app/(admin)/admin/blog/[id]/page.tsx**
    Server Component. Auth check. Next.js 15 async params: `const { id } = await params`. Fetch: `createAdminClient().from('blog_posts').select('*').eq('id', id).single()`. If error/null, `notFound()`. Render `<BlogPostForm post={post} />`.
  </action>
  <verify>
    1. `npx tsc --noEmit` exits 0.
    2. /admin/blog loads showing post list (empty on fresh install is fine).
    3. /admin/blog/new loads with empty form; title blur auto-fills slug.
    4. Create a post with published=true — appears in list with "Published" badge.
    5. Edit post — pre-filled form; save updates correctly; delete removes it.
  </verify>
  <done>Admin can create, edit, delete blog posts with Tiptap body, featured image upload, tag, and published toggle; all CRUD paths tested; no TypeScript errors.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes with zero errors.
2. Full CRUD cycle: create post → edit → publish → delete all work from admin.
3. Featured image uploads to content-images bucket and URL is saved.
4. Published posts are distinct from drafts in the list view.
</verification>

<success_criteria>
- Admin can fully manage blog posts from /admin/blog
- Slug auto-generation works (create mode only, does not overwrite in edit mode)
- Published toggle controls post visibility
- Image upload uses content-images bucket (not product-images)
- Build passes with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-content-pages/07-05-SUMMARY.md`
</output>
