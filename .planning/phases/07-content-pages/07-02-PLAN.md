---
phase: 07-content-pages
plan: "02"
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/app/(admin)/admin/pages/page.tsx
  - src/app/(admin)/_components/AboutPagesForm.tsx
  - src/app/api/admin/pages/route.ts
  - src/app/about/page.tsx
  - src/components/about/AboutStickyNav.tsx
  - src/components/about/AboutSection.tsx
autonomous: true

must_haves:
  truths:
    - "Visitor can navigate to /about and see four content sections with branded styling"
    - "Each section displays its title and body content rendered from Supabase HTML"
    - "A sticky pill nav at the top of the page highlights the active section as the user scrolls"
    - "Admin can edit each section's title, body (Tiptap), and image from /admin/pages"
    - "Admin saves trigger PUT /api/admin/pages which upserts rows into about_sections"
  artifacts:
    - path: "src/app/about/page.tsx"
      provides: "Server Component — fetches about_sections from Supabase, renders page"
    - path: "src/components/about/AboutStickyNav.tsx"
      provides: "use client sticky pill nav with IntersectionObserver active highlight"
    - path: "src/components/about/AboutSection.tsx"
      provides: "Server Component — renders Tiptap HTML with [&_tag]: Tailwind selectors"
    - path: "src/app/(admin)/admin/pages/page.tsx"
      provides: "Admin Server Component — fetches sections, passes to AboutPagesForm"
    - path: "src/app/(admin)/_components/AboutPagesForm.tsx"
      provides: "use client form — four section editors with Tiptap + image upload"
    - path: "src/app/api/admin/pages/route.ts"
      provides: "PUT handler — upserts about_sections rows via admin client"
  key_links:
    - from: "src/app/about/page.tsx"
      to: "about_sections Supabase table"
      via: "createClient().from('about_sections').select('*').order('display_order')"
      pattern: "about_sections"
    - from: "src/app/(admin)/_components/AboutPagesForm.tsx"
      to: "/api/admin/pages"
      via: "fetch PUT with sections payload"
      pattern: "api/admin/pages"
    - from: "src/components/about/AboutStickyNav.tsx"
      to: "section elements"
      via: "IntersectionObserver on getElementById for each section id"
      pattern: "IntersectionObserver"
---

<objective>
Build the complete /about page — public-facing content page with sticky pill nav, plus the /admin/pages editor so Unoma can update the brand story, mission, and contact section without touching code.

Purpose: Satisfies CONT-02 — About/Founder page tells Unoma's story and the brand mission.
Output: Public /about page with four sections + sticky nav; /admin/pages editor with Tiptap + image upload per section.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-content-pages/07-CONTEXT.md
@.planning/phases/07-content-pages/07-RESEARCH.md
@.planning/phases/07-content-pages/07-01-SUMMARY.md
@src/types/supabase.ts
@src/lib/supabase/server.ts
@src/lib/supabase/admin.ts
@src/app/(admin)/admin/settings/page.tsx
@src/app/(admin)/_components/SettingsForm.tsx
@src/app/api/admin/settings/route.ts
@src/app/(admin)/_components/RichTextEditor.tsx
@src/app/(admin)/_components/ImageUploader.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: /admin/pages editor (admin page + form + API route)</name>
  <files>
    src/app/(admin)/admin/pages/page.tsx
    src/app/(admin)/_components/AboutPagesForm.tsx
    src/app/api/admin/pages/route.ts
  </files>
  <action>
    **src/app/api/admin/pages/route.ts**
    PUT handler. Auth guard: createClient() + getUser() (same pattern as /api/admin/settings). Accept body: `{ sections: Array<{ id: string; title: string; body: string; image_url: string | null }> }`. Use createAdminClient() to upsert into about_sections with `{ onConflict: 'id' }`. Return `{ ok: true }` on success, 401 on missing auth, 500 on DB error.

    **src/app/(admin)/admin/pages/page.tsx**
    Server Component. Auth check: createClient() + getUser() + redirect('/admin/login') if null (same 3-line pattern used in every admin page). Fetch all rows: `createAdminClient().from('about_sections').select('*').order('display_order')`. If no rows exist (fresh install), seed with default sections — create four placeholder rows with ids: 'founder-story', 'brand-mission', 'why-loc-beads', 'contact', titles matching the section names, empty body, null image_url, display_order 0–3. Pass fetched (or seeded) sections as props to `<AboutPagesForm sections={sections} />`.

    **src/app/(admin)/_components/AboutPagesForm.tsx**
    `'use client'`. Props: `{ sections: AboutSection[] }`. State: one state entry per section (id, title, body, image_url). Toast state pattern: `useState<{type: 'success'|'error'; message: string} | null>(null)` + `setTimeout(3000)`.

    Render four section editors vertically — one accordion-style collapsible card per section (expanded by default). Each card has:
    - Section title text field (plain text input, not rich text)
    - Body editor: `<RichTextEditor value={section.body} onChange={...} />` with `immediatelyRender: false`
    - Image: single image upload using a simplified version of ImageUploader — one image slot (no drag-reorder needed). Use createBrowserClient() + `content-images` bucket + path `about/${section.id}/${Date.now()}-${filename}`. If content-images bucket doesn't exist at runtime, fall back gracefully with an error toast rather than crashing.
    - Show current image_url as a small preview thumbnail if set.

    Save button at the bottom sends PUT to /api/admin/pages with all four sections' current state. Show toast on success/error.

    **Critical patterns from existing code:**
    - Import RichTextEditor from `'../RichTextEditor'` (or adjust relative path to match file location)
    - Tiptap immediatelyRender: false — already set inside RichTextEditor.tsx, no action needed
    - Do NOT use `prose` class — RichTextEditor renders in the editor; this form is for editing, not display
    - Use the same admin form card styling as SettingsForm and ReviewForm (dark background, stone borders)
  </action>
  <verify>
    1. `npx tsc --noEmit` exits 0.
    2. Navigate to /admin/pages in dev server — page loads showing four section editors.
    3. Edit a section body and click Save — toast shows "Saved"; no console errors.
  </verify>
  <done>Admin can edit all four about page sections (title + rich text body + image) and save them to Supabase via PUT /api/admin/pages.</done>
</task>

<task type="auto">
  <name>Task 2: Public /about page with sticky pill nav</name>
  <files>
    src/app/about/page.tsx
    src/components/about/AboutStickyNav.tsx
    src/components/about/AboutSection.tsx
  </files>
  <action>
    **src/components/about/AboutSection.tsx**
    Server Component (no `'use client'`). Props: `{ section: AboutSection }`. Render a `<section id={section.id}>` wrapper containing:
    - Section title as `<h2>` with `font-heading text-2xl lg:text-3xl font-bold text-charcoal mb-6`
    - If section.image_url is set, render `<Image>` from next/image with appropriate sizing (fill or fixed width)
    - Body HTML rendered via `dangerouslySetInnerHTML={{ __html: section.body }}` inside a div with `[&_h2]:font-heading [&_h2]:text-2xl [&_h2]:font-bold [&_h2]:mb-4 [&_p]:mb-4 [&_p]:leading-relaxed [&_p]:text-charcoal/80 [&_ul]:list-disc [&_ul]:pl-6 [&_ul]:mb-4 [&_a]:text-gold [&_a]:underline [&_a]:hover:text-gold/80`
    - Do NOT use `prose` class — @tailwindcss/typography is not installed
    - For the 'contact' section specifically, render a WhatsApp CTA button below the body: `<a href="https://wa.me/2348000000000" ...>` (same placeholder as checkout; real number added in Phase 10) with the existing site WhatsApp button style

    **src/components/about/AboutStickyNav.tsx**
    `'use client'`. Implement the IntersectionObserver sticky pill nav exactly as specified in the research:
    ```
    const SECTIONS = [
      { id: 'founder-story', label: 'Founder Story' },
      { id: 'brand-mission', label: 'Brand Mission' },
      { id: 'why-loc-beads', label: 'Why Loc Beads' },
      { id: 'contact', label: 'Contact' },
    ]
    ```
    Use `rootMargin: '-30% 0px -60% 0px'` to prevent scroll jank. Active section gets `bg-gold text-white` pill; inactive gets `text-charcoal/60 hover:text-charcoal`. Outer nav is `sticky top-4 z-30 flex justify-center`. Inner container is `flex gap-2 bg-white/80 backdrop-blur-sm rounded-full px-3 py-2 shadow-md border border-stone-200`.

    **src/app/about/page.tsx**
    Server Component (no auth required — public page). Use `createClient()` (public client, not admin). Fetch: `.from('about_sections').select('*').order('display_order')`. If data is empty or null, render the page with sensible placeholder copy rather than crashing.

    Page structure:
    ```
    <main>
      <div className="max-w-3xl mx-auto px-4 py-12">
        <h1 className="font-heading text-4xl ...">About Twinkle Locs</h1>
        <AboutStickyNav />
        <div className="mt-12 space-y-24">
          {sections.map(section => <AboutSection key={section.id} section={section} />)}
        </div>
      </div>
    </main>
    ```

    The `<AboutStickyNav />` is a `use client` leaf island — `page.tsx` stays a Server Component. Pass no props to AboutStickyNav (it reads DOM directly via getElementById).
  </action>
  <verify>
    1. `npx tsc --noEmit` exits 0.
    2. Visit /about in dev server — page renders with four sections and sticky pill nav.
    3. Scroll through the page — active pill updates as sections enter the viewport.
    4. Contact section shows a WhatsApp CTA button.
  </verify>
  <done>Public /about page renders all four sections from Supabase; sticky nav highlights active section on scroll; Contact section has WhatsApp CTA; no TypeScript errors.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes with zero errors.
2. /about page loads and renders correctly with sticky pill nav.
3. /admin/pages loads, shows four section editors, save works.
4. No `prose` class used anywhere in this plan's files.
5. No `window.location` calls at render time.
</verification>

<success_criteria>
- CONT-02: /about page displays brand story and mission
- Admin can edit all four sections from /admin/pages
- Sticky nav works with IntersectionObserver rootMargin trick
- Build passes with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/07-content-pages/07-02-SUMMARY.md`
</output>
