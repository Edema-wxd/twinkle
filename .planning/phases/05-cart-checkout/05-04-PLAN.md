---
phase: 05-cart-checkout
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/cart/CartDrawer.tsx
  - src/components/cart/CartLineItem.tsx
  - src/components/layout/Header.tsx
autonomous: true

must_haves:
  truths:
    - "Cart drawer slides in from the right when isDrawerOpen is true"
    - "Cart drawer shows all line items with thumbnail, name, size, pack size, thread colour swatch+label, unit price, +/- quantity controls, remove button"
    - "Drawer footer shows subtotal and a Checkout CTA button linking to /checkout"
    - "Empty state shows 'Your cart is empty' message and a Shop Now link to /catalog"
    - "Header cart icon shows the total item count (sum of all line quantities)"
    - "Drawer closes on clicking outside overlay or pressing Escape"
    - "Quantity +/- controls dispatch UPDATE_QTY; remove button dispatches REMOVE_ITEM"
  artifacts:
    - path: "src/components/cart/CartDrawer.tsx"
      provides: "Slide-out cart drawer component"
      exports: ["CartDrawer"]
    - path: "src/components/cart/CartLineItem.tsx"
      provides: "Individual line item row for drawer and cart page"
      exports: ["CartLineItem"]
  key_links:
    - from: "src/components/cart/CartDrawer.tsx"
      to: "src/lib/cart/CartContext.tsx"
      via: "useCart hook — reads state.items, state.isDrawerOpen, dispatches CLOSE_DRAWER/UPDATE_QTY/REMOVE_ITEM"
      pattern: "useCart"
    - from: "src/components/layout/Header.tsx"
      to: "src/lib/cart/CartContext.tsx"
      via: "useCart hook — reads total item count for badge"
      pattern: "useCart"
---

<objective>
Build the cart drawer (slide-out panel) and integrate CartDrawer into the app via Header rendering and the isDrawerOpen context flag.

Purpose: The cart drawer is the primary cart interaction point — it opens automatically when items are added and allows quantity adjustments before proceeding to checkout.
Output: Functional slide-out drawer rendering live cart state with full line item controls.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-cart-checkout/05-CONTEXT.md
@src/lib/cart/CartContext.tsx
@src/lib/cart/types.ts
@src/lib/cart/threadColours.ts
@src/components/layout/Header.tsx
@src/components/layout/MobileDrawer.tsx
@.planning/phases/05-cart-checkout/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CartLineItem component</name>
  <files>
    src/components/cart/CartLineItem.tsx
  </files>
  <action>
Create `src/components/cart/` directory. Create `src/components/cart/CartLineItem.tsx` with `'use client'` directive.

Props interface:
```typescript
interface CartLineItemProps {
  item: CartItem
  lineKey: string
  onUpdateQty: (key: string, qty: number) => void
  onRemove: (key: string) => void
}
```

Layout (horizontal flex row, `py-3 border-b border-charcoal/10 last:border-b-0`):
- Left: product thumbnail — `<Image>` from next/image, src={item.imageUrl}, alt={item.productName}, width=64, height=64, className="w-16 h-16 object-contain rounded-md bg-stone flex-shrink-0"
- Middle (flex-1, ml-3):
  - Product name: `font-heading text-sm font-semibold text-cocoa`
  - Variant/pack line: `font-body text-xs text-charcoal/60 mt-0.5` — e.g. "4mm · Pack of 50"
  - Thread colour line (omit if item.isTool): `font-body text-xs text-charcoal/60 flex items-center gap-1.5 mt-0.5` — small circle swatch `w-3 h-3 rounded-full inline-block` with `style={{ backgroundColor: THREAD_COLOURS.find(c => c.id === item.threadColour)?.hex }}` followed by the colour label
  - Price: `font-heading text-sm font-semibold text-gold mt-1` — `₦{item.unitPrice.toLocaleString()}`
- Right (flex-col items-end gap-2 ml-3):
  - Quantity controls: small row with `-` button, qty display, `+` button. `-` calls onUpdateQty(key, item.quantity - 1). `+` calls onUpdateQty(key, item.quantity + 1). `+` disabled when quantity >= 10. Button style: `w-6 h-6 rounded border border-charcoal/20 text-charcoal text-sm font-body flex items-center justify-center hover:border-gold transition-colors`. Disabled `+`: `opacity-40 cursor-not-allowed`.
  - Remove button: small `×` text button, `text-xs text-charcoal/40 hover:text-terracotta transition-colors font-body`, calls onRemove(key)

Import THREAD_COLOURS from @/lib/cart/threadColours. Import CartItem from @/lib/cart/types. Import Image from next/image. Import lineKey from @/lib/cart/cartReducer.
  </action>
  <verify>
`npx tsc --noEmit` — zero errors.
  </verify>
  <done>
CartLineItem renders thumbnail, product name, variant+pack info, optional thread colour swatch, price, quantity controls (+/- up to 10), and remove button. TypeScript compiles.
  </done>
</task>

<task type="auto">
  <name>Task 2: CartDrawer component + Header cart icon badge</name>
  <files>
    src/components/cart/CartDrawer.tsx
    src/components/layout/Header.tsx
  </files>
  <action>
Create `src/components/cart/CartDrawer.tsx` with `'use client'` directive.

Import useCart from @/lib/cart/CartContext. Import CartLineItem from ./CartLineItem. Import lineKey from @/lib/cart/cartReducer. Import Link from next/link. Import useEffect from react (for Escape key listener).

Derive from cart state:
- `items` = state.items
- `isOpen` = state.isDrawerOpen
- `totalItems` = items.reduce((sum, i) => sum + i.quantity, 0)
- `subtotal` = items.reduce((sum, i) => sum + i.unitPrice * i.quantity, 0)

Structure:
- Outer wrapper: fixed inset-0 z-50, only rendered when isOpen (or always rendered but hidden — use `pointer-events-none` + translate when closed for smooth animation)
- Backdrop: `fixed inset-0 bg-black/40 transition-opacity` — clicking it dispatches CLOSE_DRAWER
- Drawer panel: `fixed right-0 top-0 h-full w-full max-w-sm bg-cream shadow-2xl flex flex-col transition-transform duration-300` — slides in via `translate-x-0` when open, `translate-x-full` when closed
- Header row (drawer): "Your Cart" heading (font-display text-xl text-cocoa) + close button (×, `text-charcoal/60 hover:text-cocoa text-2xl leading-none`)
- Scrollable items area (flex-1 overflow-y-auto px-4): render CartLineItem for each item; if items.length === 0 show empty state (centered: icon or illustration, "Your cart is empty" in font-body text-charcoal/60, Link to /catalog "Shop Now" in underline + gold colour)
- Footer (sticky bottom, border-t border-charcoal/10 p-4 bg-cream):
  - Subtotal row: "Subtotal" label + `₦{subtotal.toLocaleString()}` in font-heading font-semibold text-cocoa. Note "Shipping calculated at checkout" in xs text
  - Checkout CTA: Link to /checkout, full-width button style `bg-gold text-cocoa font-heading font-semibold py-3 rounded-lg hover:bg-terracotta hover:text-cream transition-colors w-full text-center block` — hidden (or pointer-events-none) if cart is empty

Add Escape key handler: useEffect that adds keydown listener; on Escape, dispatch CLOSE_DRAWER; cleanup on unmount.

Render CartDrawer inside layout.tsx (or in Providers.tsx alongside CartProvider): Add `<CartDrawer />` as a sibling of children inside the Providers wrapper, so it's always mounted and can respond to state changes.

Update `src/components/layout/Header.tsx`:
- Import useCart.
- Derive totalItems from state: `const { state } = useCart()` and `const totalItems = state.items.reduce((sum, i) => sum + i.quantity, 0)`.
- Find the cart icon in the header (currently a no-op or Link to /cart) and add a badge: if totalItems > 0, show a small `bg-gold text-cocoa rounded-full text-xs font-heading font-semibold w-5 h-5 flex items-center justify-center absolute -top-1 -right-1` badge showing totalItems (cap display at 9+ if > 9).
- The cart icon's onClick should dispatch OPEN_DRAWER to open the cart drawer (in addition to or instead of navigating to /cart). Keep the cart icon as a button that opens the drawer; /cart page link remains accessible via the full cart page.
  </action>
  <verify>
`npx tsc --noEmit` — zero errors.
`npm run build` — passes.
Navigate to product page, add item — cart drawer slides in from right. Drawer shows line item with thumbnail, colour swatch, price, quantity controls. Pressing Escape closes it. Clicking backdrop closes it.
  </verify>
  <done>
CartDrawer renders with slide animation, shows items or empty state, has working quantity controls and remove buttons, shows subtotal and Checkout CTA. Header shows item count badge. Escape key closes drawer.
  </done>
</task>

</tasks>

<verification>
`npx tsc --noEmit` — zero errors.
`npm run build` — passes.
Add item from product page — drawer opens automatically.
+/- controls update quantity; - at quantity 1 removes item.
Clicking × remove button removes the line.
When all items removed, empty state with Shop Now link appears.
Header badge shows correct count.
</verification>

<success_criteria>
- CartDrawer slides in from right with smooth CSS transition
- Line items show thumbnail, name, size, pack, thread colour swatch+label (skipped for Tools), price, +/- controls (max 10), remove button
- Footer shows subtotal and Checkout CTA Link to /checkout
- Empty state shows "Your cart is empty" + Shop Now link
- Header badge shows total item quantity (capped at 9+)
- Escape and backdrop click both close the drawer
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-cart-checkout/05-04-SUMMARY.md`
</output>
