---
phase: 05-cart-checkout
plan: 09
type: execute
wave: 4
depends_on: ["05-03", "05-04", "05-05", "05-07", "05-08"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Full purchase flow completes end-to-end: add → drawer → /cart → /checkout → Paystack → /orders/[reference]"
    - "Cart persists across page refresh"
    - "All 8 CART-0x requirements are satisfied"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification of the complete cart and checkout flow. No new code — this plan confirms Phase 5 success criteria are fully met before marking the phase complete.

Purpose: Catch integration issues between plans (e.g. cart context boundary, Paystack test payment, webhook delivery) that only surface when the full flow runs together.
Output: Verified Phase 5 completion.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-cart-checkout/05-CONTEXT.md
@.planning/phases/05-cart-checkout/05-07-SUMMARY.md
@.planning/phases/05-cart-checkout/05-08-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 5 Cart and Checkout system:
- Cart context with localStorage persistence (Plan 01)
- Supabase orders/order_items schema (Plan 02)
- Thread colour picker + Add to Cart wiring (Plan 03)
- Cart drawer with line item controls (Plan 04)
- /cart page (Plan 05)
- Paystack webhook handler (Plan 06)
- Two-step checkout with Paystack popup (Plan 07)
- Order confirmation page with Realtime polling (Plan 08)
  </what-built>
  <how-to-verify>
Run `npm run dev` to start the dev server.

**Test 1 — Cart persistence:**
1. Go to any product page (e.g. /catalog/24k-gold-beads)
2. Select a size, select a pack size, select a thread colour
3. Click "Add to Cart" — cart drawer should slide in from the right automatically
4. Confirm line item in drawer shows: product name, size, pack of N, thread colour swatch + label, unit price
5. Close the drawer, refresh the page
6. Click the cart icon — drawer reopens with the same item (localStorage persistence confirmed)

**Test 2 — Cart drawer controls:**
1. In the cart drawer, click + to increase quantity to 3
2. Click - to decrease back to 1
3. Click the × remove button — item removed, empty state shown

**Test 3 — Multiple line items + merge:**
1. Add the same product/size/pack/colour twice — quantity should increase to 2 (merge, not new line)
2. Add same product/size/pack but a DIFFERENT colour — new separate line item added
3. Cart shows 2 lines correctly

**Test 4 — /cart page:**
1. Navigate to /cart — same line items shown as in drawer
2. Quantity controls work on /cart page
3. Remove all items — empty state shown with Continue Shopping link

**Test 5 — Checkout form:**
1. Add an item, go to /checkout
2. Test International toggle — WhatsApp message appears, no payment button
3. Switch back to Nigeria
4. Leave a field blank, click Review Order — validation error shown
5. Fill all fields, select Lagos as state, click Review Order
6. Step 2 shows: items, subtotal, Shipping (Lagos) ₦3,000, total = subtotal + 3000
7. Click Back — returns to Step 1 with data preserved

**Test 6 — Paystack test payment (requires NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY set in .env.local):**
1. Complete Step 1 with any Nigerian state (non-Lagos to confirm ₦4,500 shipping)
2. On Step 2, click Pay — Paystack popup should open
3. Close the popup without paying — error banner appears "Payment was not completed — please try again"
4. Cart is NOT cleared — items still in drawer/localStorage

**Test 7 — Shears (Tools) product:**
1. Go to /catalog/shears
2. Confirm no thread colour selector shown
3. Add to Cart button is immediately enabled (no colour required)
4. Cart shows Shears without any thread colour line

**Test 8 — Build check:**
Run `npm run build` — must complete without errors.
  </how-to-verify>
  <resume-signal>
Type "approved" if all 8 tests pass, or describe any failures in detail so they can be fixed.
  </resume-signal>
</task>

</tasks>

<verification>
All tests in the checkpoint above pass.
`npm run build` succeeds.
CART-01 through CART-08 requirements all satisfied.
</verification>

<success_criteria>
- CART-01: Visitor can add product variant to cart — confirmed
- CART-02: Cart drawer with edit controls — confirmed
- CART-03: /cart page with edit controls — confirmed
- CART-04: Guest checkout form (name, email, address) — confirmed
- CART-05: Paystack popup opens with correct amount in NGN — confirmed
- CART-06: Domestic shipping rate calculated from state — confirmed
- CART-07: International toggle shows WhatsApp contact flow — confirmed
- CART-08: Order confirmation page after payment — pending (requires live webhook test)
- `npm run build` passes — confirmed
</success_criteria>

<output>
After completion, create `.planning/phases/05-cart-checkout/05-09-SUMMARY.md`
Update STATE.md: Phase 5 complete, advance current position to Phase 6.
</output>
