---
phase: 05-cart-checkout
plan: 05
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/cart/page.tsx
autonomous: true

must_haves:
  truths:
    - "Visiting /cart shows all cart line items with the same detail as the drawer (thumbnail, size, pack, thread colour, price, quantity controls)"
    - "Quantity controls and remove work the same as in the drawer"
    - "Page shows a subtotal and a Checkout button linking to /checkout"
    - "Empty cart state on /cart shows a message and link back to catalog"
  artifacts:
    - path: "src/app/cart/page.tsx"
      provides: "Dedicated cart page at /cart"
      contains: "CartLineItem"
  key_links:
    - from: "src/app/cart/page.tsx"
      to: "src/lib/cart/CartContext.tsx"
      via: "useCart hook reads items and dispatches UPDATE_QTY/REMOVE_ITEM"
      pattern: "useCart"
---

<objective>
Build the dedicated /cart page — a full-page view of the cart accessible from the header cart icon and from the drawer Checkout CTA.

Purpose: Provides a persistent, bookmarkable URL for cart review before checkout. Reuses CartLineItem component from the drawer so no logic is duplicated.
Output: /cart page rendering cart state with quantity controls and checkout navigation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-cart-checkout/05-CONTEXT.md
@src/lib/cart/CartContext.tsx
@src/lib/cart/types.ts
@src/components/cart/CartLineItem.tsx
@.planning/phases/05-cart-checkout/05-01-SUMMARY.md
@.planning/phases/05-cart-checkout/05-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: /cart page</name>
  <files>
    src/app/cart/page.tsx
  </files>
  <action>
Create `src/app/cart/` directory. Create `src/app/cart/page.tsx` with `'use client'` directive (must be client since it reads cart context).

Import useCart from @/lib/cart/CartContext. Import CartLineItem from @/components/cart/CartLineItem. Import lineKey from @/lib/cart/cartReducer. Import Link from next/link.

Layout: max-w-2xl mx-auto px-4 py-8

Page heading: "Your Cart" — `font-display text-3xl text-cocoa mb-8`

Two-column layout on md+: left column (cart items, md:col-span-2) + right column (order summary, md:col-span-1). On mobile: stacked.

Left column (items):
- If items.length === 0: centered empty state — "Your cart is empty" in font-body text-charcoal/60, Link to /catalog styled as gold underline button "Continue Shopping"
- If items.length > 0: render CartLineItem for each item, with onUpdateQty and onRemove dispatching UPDATE_QTY and REMOVE_ITEM respectively

Right column (order summary — only shown when items.length > 0):
- Section heading "Order Summary" in font-heading text-lg text-cocoa mb-4
- Subtotal row: "Subtotal" label + `₦{subtotal.toLocaleString()}` — font-heading font-semibold
- "Shipping calculated at checkout" — font-body text-xs text-charcoal/50 mt-1 mb-4
- Divider
- Total row (same as subtotal for now — shipping TBD): "Total" + same amount in text-xl
- Note text: "Taxes included where applicable" — xs text-charcoal/40
- Checkout button: Link to /checkout, full-width, `bg-gold text-cocoa font-heading font-semibold py-3 px-6 rounded-lg hover:bg-terracotta hover:text-cream transition-colors w-full text-center block mt-4`
- Below button: Link to /catalog "Continue Shopping" — text-sm text-charcoal/60 hover:text-cocoa underline text-center block mt-3

Export `metadata` for the page title: `export const metadata = { title: 'Your Cart — Twinkle Locs' }` — but since this is 'use client', do NOT export metadata (only Server Components can export metadata). Skip metadata for now; SEO phase handles this.
  </action>
  <verify>
`npx tsc --noEmit` — zero errors.
Navigate to /cart — page loads without error.
Add items from a product page, then navigate to /cart — items appear with controls.
Remove all items — empty state shown.
  </verify>
  <done>
/cart page renders cart state. Quantity controls work. Empty state shows. Checkout CTA links to /checkout. TypeScript compiles without errors.
  </done>
</task>

</tasks>

<verification>
`npx tsc --noEmit` — zero errors.
Visit /cart with empty cart — "Your cart is empty" + Continue Shopping link.
Add items from product page, visit /cart — items listed with correct variant info and thread colour.
+/- controls update quantities. Remove button removes items.
Checkout button links to /checkout.
</verification>

<success_criteria>
- /cart page renders at /cart without errors
- Shows all cart items using CartLineItem component
- Quantity controls and remove work correctly
- Subtotal calculated from items
- Checkout CTA links to /checkout
- Empty state with Continue Shopping link
- `npx tsc --noEmit` passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-cart-checkout/05-05-SUMMARY.md`
</output>
