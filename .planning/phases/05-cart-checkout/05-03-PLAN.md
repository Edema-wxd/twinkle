---
phase: 05-cart-checkout
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/components/product/ProductDetailClient.tsx
autonomous: true

must_haves:
  truths:
    - "Thread colour swatches are interactive — clicking selects a colour with a visual border indicator"
    - "Add to Cart button is disabled (shows 'Select Thread Colour') until a colour is selected for bead products"
    - "Add to Cart dispatches ADD_ITEM to cart context with all 9 required fields"
    - "Dispatching ADD_ITEM auto-opens the cart drawer (OPEN_DRAWER handled inside ADD_ITEM in reducer)"
    - "Tools products (Shears) skip thread colour entirely and Add to Cart is immediately enabled"
    - "Thread colour selection is reset when the user switches bead size variant"
  artifacts:
    - path: "src/components/product/ProductDetailClient.tsx"
      provides: "Interactive thread colour picker + wired Add to Cart"
      contains: "selectedThreadColour"
  key_links:
    - from: "src/components/product/ProductDetailClient.tsx"
      to: "src/lib/cart/CartContext.tsx"
      via: "useCart hook, dispatch ADD_ITEM"
      pattern: "useCart|dispatch.*ADD_ITEM"
    - from: "src/components/product/ProductDetailClient.tsx"
      to: "src/lib/cart/threadColours.ts"
      via: "THREAD_COLOURS import for swatch rendering"
      pattern: "THREAD_COLOURS"
---

<objective>
Wire the thread colour selector and Add to Cart button in ProductDetailClient to the cart context. Replaces the Phase 4 decorative placeholder with fully functional selection state.

Purpose: The product detail page is the entry point to the cart. Without this wiring, no items can be added.
Output: ProductDetailClient dispatches ADD_ITEM with complete line item data; cart drawer auto-opens on add.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-cart-checkout/05-CONTEXT.md
@.planning/phases/05-cart-checkout/05-RESEARCH.md
@src/components/product/ProductDetailClient.tsx
@src/lib/cart/CartContext.tsx
@src/lib/cart/types.ts
@src/lib/cart/threadColours.ts
@.planning/phases/05-cart-checkout/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread colour state + interactive swatch picker in ProductDetailClient</name>
  <files>
    src/components/product/ProductDetailClient.tsx
  </files>
  <action>
Read src/components/product/ProductDetailClient.tsx in full before making any changes.

Add the following to ProductDetailClient:

1. Import `useCart` from `@/lib/cart/CartContext`.
2. Import `THREAD_COLOURS` from `@/lib/cart/threadColours`.

3. Add `selectedThreadColour` state alongside the existing variant/tier state:
   ```typescript
   const [selectedThreadColour, setSelectedThreadColour] = useState<string | null>(
     product.material === 'Tools' ? '' : null
   )
   ```
   Tools products auto-initialise to empty string (enabled); bead products start as null (disabled).

4. Reset thread colour when variant changes — update `handleVariantChange` to also call `setSelectedThreadColour(product.material === 'Tools' ? '' : null)`.

5. Replace the decorative thread colour section (lines around 132–150 — the block with hardcoded `bg-[#1A1A1A]` swatches and "Colour selection coming soon") with an interactive picker using THREAD_COLOURS:
   - Section heading: "Thread Colour" with a "Required" badge in terracotta if selectedThreadColour is null
   - Five swatch buttons from THREAD_COLOURS, each with: `type="button"`, `onClick={() => setSelectedThreadColour(colour.id)}`, `title={colour.label}`, `aria-label={colour.label}`, `aria-pressed={selectedThreadColour === colour.id}`
   - Selected swatch: `border-cocoa scale-110`; unselected: `border-transparent hover:border-charcoal/40`
   - Swatch size: `w-8 h-8 rounded-full border-2 transition-all` with `style={{ backgroundColor: colour.hex }}`
   - Below swatches: if selectedThreadColour is non-null, show the colour label in `text-xs text-charcoal/60 mt-2 font-body`

6. Add `canAddToCart` derived value:
   ```typescript
   const canAddToCart = product.material === 'Tools' || selectedThreadColour !== null
   ```

7. Add `handleAddToCart` function that calls `dispatch({ type: 'ADD_ITEM', payload: { ... } })` with all CartItem fields: productId (product.id), variantId (selectedVariantId), tierQty (selectedTierQty), threadColour (product.material === 'Tools' ? '' : selectedThreadColour!), productName (product.name), variantName (selectedVariant?.name ?? ''), unitPrice (displayPrice), imageUrl (product.image), isTool (product.material === 'Tools'), quantity: 1.

8. Update the Add to Cart button:
   - `onClick={handleAddToCart}`
   - `disabled={!canAddToCart}`
   - When canAddToCart: `bg-gold text-cocoa hover:bg-terracotta hover:text-cream`
   - When !canAddToCart: `bg-charcoal/20 text-charcoal/40 cursor-not-allowed`
   - Button text: `canAddToCart ? 'Add to Cart' : 'Select Thread Colour'`
   - Remove the `mt-8` class — keep the existing bottom margin pattern consistent with other sections
  </action>
  <verify>
Run: `npx tsc --noEmit`
Visually confirm on `/catalog/24k-gold-beads`: thread colour swatches appear, clicking one selects it (border), button becomes active showing "Add to Cart", clicking Add to Cart with a colour selected dispatches to cart (visible in React DevTools or by checking localStorage `twinkle_cart`).
Visually confirm on `/catalog/shears`: no thread colour section, Add to Cart immediately enabled.
  </verify>
  <done>
Thread colour picker is interactive with 5 locked colours. Add to Cart is disabled until colour is selected (for bead products). Clicking Add to Cart dispatches ADD_ITEM to cart context. Tools products skip colour selection. TypeScript compiles without errors.
  </done>
</task>

</tasks>

<verification>
`npx tsc --noEmit` — zero errors.
Navigate to a bead product page — thread colour swatches visible, selecting one enables the button.
Navigate to /catalog/shears — no thread colour section, button is immediately enabled.
Click Add to Cart — check localStorage `twinkle_cart` in DevTools to confirm item was added.
</verification>

<success_criteria>
- Thread colour picker renders 5 correct colour swatches (red, black, light-brown, blonde, dark-brown)
- Add to Cart disabled until colour selected on bead products
- Add to Cart immediately enabled on Tools products
- Dispatches ADD_ITEM with all CartItem fields on click
- TypeScript compiles without errors
- Variant switch resets thread colour selection
</success_criteria>

<output>
After completion, create `.planning/phases/05-cart-checkout/05-03-SUMMARY.md`
</output>
