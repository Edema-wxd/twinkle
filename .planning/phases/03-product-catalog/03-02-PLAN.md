---
phase: 03-product-catalog
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/catalog/CatalogProductCard.tsx
  - src/components/catalog/FilterBar.tsx
  - src/components/catalog/FilterDrawer.tsx
  - src/components/catalog/SearchInput.tsx
autonomous: true

must_haves:
  truths:
    - "CatalogProductCard renders product image, name, category badge, price, stock status, and links to /catalog/[slug]"
    - "FilterBar shows category chips (All, Gold, Silver, Crystal, Tools) and sort dropdown"
    - "FilterDrawer wraps MobileDrawer with the same category chips + sort controls"
    - "SearchInput is a controlled text input that calls onSearch on every keystroke"
    - "CatalogProductCard has NO 'use client' directive — it is a pure component with Link navigation"
  artifacts:
    - path: "src/components/catalog/CatalogProductCard.tsx"
      provides: "Product card for catalog grid — Link-based navigation"
      exports: ["CatalogProductCard"]
    - path: "src/components/catalog/FilterBar.tsx"
      provides: "Desktop filter chips + sort dropdown"
      exports: ["FilterBar"]
    - path: "src/components/catalog/FilterDrawer.tsx"
      provides: "Mobile slide-in filter panel"
      exports: ["FilterDrawer"]
    - path: "src/components/catalog/SearchInput.tsx"
      provides: "Controlled search text input"
      exports: ["SearchInput"]
  key_links:
    - from: "src/components/catalog/CatalogProductCard.tsx"
      to: "/catalog/[slug]"
      via: "Next.js Link href"
      pattern: "href.*catalog.*slug"
    - from: "src/components/catalog/FilterDrawer.tsx"
      to: "src/components/layout/MobileDrawer.tsx"
      via: "import and wrap MobileDrawer"
      pattern: "import.*MobileDrawer"
---

<objective>
Build the four presentational/UI components that CatalogClient will compose: CatalogProductCard, FilterBar, FilterDrawer, and SearchInput.

Purpose: CatalogClient (Plan 03) needs clean, typed sub-components with well-defined props interfaces. Building them separately keeps Plan 03 focused on state logic.
Output: src/components/catalog/ directory with 4 component files — all typed, all following existing codebase conventions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-product-catalog/03-CONTEXT.md
@.planning/phases/03-product-catalog/03-RESEARCH.md
@.planning/phases/03-product-catalog/03-01-SUMMARY.md

@src/lib/types/product.ts
@src/app/globals.css
@src/components/layout/MobileDrawer.tsx
@src/components/home/ProductCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CatalogProductCard — pure product card with Link navigation</name>
  <files>src/components/catalog/CatalogProductCard.tsx</files>
  <action>
    Create src/components/catalog/CatalogProductCard.tsx.

    DO NOT add 'use client' — this component uses only Next.js Link for navigation, no event handlers.

    Props interface CatalogProductCardProps: { product: Product }

    Import: Product from '@/lib/types/product', Image from 'next/image', Link from 'next/link'

    Layout (matches ProductCard structure but uses Link wrapper, adds badge + stock):
    - Outer: <Link href={`/catalog/${product.slug}`}> wrapping an <article>
    - article classes: bg-stone rounded-xl overflow-hidden hover:-translate-y-1 hover:shadow-lg transition-all duration-200 cursor-pointer
    - Image block: relative aspect-square w-full, fill Image, sizes same as ProductCard
    - Content block p-4:
      1. Category badge (top of content, before name):
         Span with small rounded pill. Color map:
         Gold → bg-gold/20 text-cocoa
         Silver → bg-stone border border-charcoal/20 text-charcoal
         Crystal → bg-forest/10 text-forest
         Tools → bg-terracotta/10 text-terracotta
         Classes: inline-block text-xs font-body font-medium px-2 py-0.5 rounded-full mb-2
      2. Product name: font-heading font-semibold text-cocoa text-base mb-1
      3. Price: "From ₦{product.price_min.toLocaleString()}" — font-body text-sm text-charcoal/60 mb-3
      4. Stock status pill (bottom of card):
         Derive: const inStock = product.variants.some(v => v.in_stock)
         If in stock: small dot indicator — "In Stock" with a green dot (text-forest)
         If out of stock: "Out of Stock" muted badge (text-terracotta/80)
         Classes: flex items-center gap-1.5 font-body text-xs

    The MATERIAL_COLOURS record and stock helper should be defined inside the file (not exported).
    No onClick, no button elements.
  </action>
  <verify>npx tsc --noEmit — zero errors on the new file</verify>
  <done>CatalogProductCard renders with category badge, price "From ₦X", stock indicator, and links to /catalog/[slug] — no 'use client' directive</done>
</task>

<task type="auto">
  <name>Task 2: Create FilterBar, FilterDrawer, and SearchInput components</name>
  <files>
    src/components/catalog/FilterBar.tsx
    src/components/catalog/FilterDrawer.tsx
    src/components/catalog/SearchInput.tsx
  </files>
  <action>
    Create all three files. Each requires 'use client'.

    --- FilterBar.tsx ---
    Props:
      activeCategory: Category (type Category = 'All' | 'Gold' | 'Silver' | 'Crystal' | 'Tools')
      onCategoryChange: (category: Category) => void
      sortOrder: SortOrder (type SortOrder = 'price_asc' | 'price_desc' | 'latest')
      onSortChange: (sort: SortOrder) => void

    Export the Category and SortOrder types from this file so CatalogClient can import them.

    Layout: flex flex-wrap items-center gap-3
    - Category chips: one chip per category in ['All', 'Gold', 'Silver', 'Crystal', 'Tools']
      Active chip: bg-cocoa text-cream
      Inactive chip: bg-stone text-charcoal border border-charcoal/20 hover:bg-cocoa/10
      Chip classes: px-4 py-1.5 rounded-full font-body text-sm font-medium transition-colors cursor-pointer
    - Sort dropdown: <select> with options:
        value="latest" → "Latest"
        value="price_asc" → "Price: Low to High"
        value="price_desc" → "Price: High to Low"
      Select classes: ml-auto font-body text-sm bg-stone border border-charcoal/20 rounded-lg px-3 py-1.5 text-charcoal cursor-pointer

    --- FilterDrawer.tsx ---
    Props:
      isOpen: boolean
      onClose: () => void
      activeCategory: Category
      onCategoryChange: (category: Category) => void
      sortOrder: SortOrder
      onSortChange: (sort: SortOrder) => void

    Import Category, SortOrder from './FilterBar'
    Import MobileDrawer from '@/components/layout/MobileDrawer'

    Renders MobileDrawer with isOpen/onClose, passing filter content as children:
    - Heading: "Filter & Sort" font-display text-lg text-cocoa mb-6
    - Category section label: "Category" font-heading text-xs uppercase tracking-widest text-charcoal/60 mb-3
    - Category chips: vertical stack (flex flex-col gap-2) using same chip styling as FilterBar
    - Divider: border-t border-charcoal/10 my-6
    - Sort section label: "Sort By" — same label style
    - Sort radio buttons: flex flex-col gap-2, each: <button> with same chip styling as active/inactive chips

    --- SearchInput.tsx ---
    Props:
      value: string
      onChange: (value: string) => void
      placeholder?: string

    Renders a search input wrapped in a relative div:
    - Container: relative flex-1 min-w-0
    - Input: w-full font-body text-sm bg-stone border border-charcoal/20 rounded-lg px-4 py-2 text-charcoal placeholder:text-charcoal/40 focus:outline-none focus:ring-2 focus:ring-gold/40
    - Optional: magnifying glass icon as absolute left element (use inline SVG or omit — Claude's discretion)
    - onChange calls props.onChange(e.target.value) on every keystroke — no debounce needed for 6 products
    - Default placeholder: "Search products…"

    Run npx tsc --noEmit after creating all three files.
  </action>
  <verify>npx tsc --noEmit — zero errors on all three files; no build errors from existing files</verify>
  <done>
    - FilterBar.tsx renders category chips and sort dropdown with correct active states; exports Category and SortOrder types
    - FilterDrawer.tsx wraps MobileDrawer with filter content as children
    - SearchInput.tsx is a controlled input that fires onChange on every keystroke
    - All three have 'use client' directive
    - Zero TypeScript errors
  </done>
</task>

</tasks>

<verification>
- npx tsc --noEmit passes (zero errors)
- src/components/catalog/ directory contains: CatalogProductCard.tsx, FilterBar.tsx, FilterDrawer.tsx, SearchInput.tsx
- CatalogProductCard has no 'use client' directive
- FilterBar, FilterDrawer, SearchInput each have 'use client'
- FilterBar exports Category and SortOrder types
- Category badge colour map handles all 4 material values
- Stock indicator correctly derives in_stock from variants array
</verification>

<success_criteria>
- 4 components created with correct prop interfaces
- CatalogProductCard is a pure server-renderable component (no 'use client')
- FilterDrawer reuses existing MobileDrawer (not reimplemented)
- All components follow existing Tailwind v4 token conventions (bg-gold, text-cocoa, etc.)
- Zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-product-catalog/03-02-SUMMARY.md`
</output>
