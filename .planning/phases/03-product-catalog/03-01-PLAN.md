---
phase: 03-product-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types/product.ts
  - src/lib/mock/products.ts
  - src/components/home/FeaturedProductsSection.tsx
  - src/components/home/AddToCartModal.tsx
  - src/components/home/ProductCard.tsx
autonomous: true

must_haves:
  truths:
    - "Product type has id, name, slug, description, image, material, is_featured, variants, price_min, price_max, created_at"
    - "CATALOG_PRODUCTS array contains all 6 products including Crystal Clear Beads and Shears"
    - "Each product has a material field: Gold | Silver | Crystal | Tools"
    - "Each product has a created_at ISO string for Latest sort"
    - "FeaturedProductsSection still works after type swap (homepage not broken)"
  artifacts:
    - path: "src/lib/types/product.ts"
      provides: "Canonical Product, ProductVariant, ProductMaterial types"
      exports: ["Product", "ProductVariant", "ProductMaterial"]
    - path: "src/lib/mock/products.ts"
      provides: "CATALOG_PRODUCTS (6) and FEATURED_PRODUCTS (4) arrays"
      contains: "Crystal Clear Beads, Shears"
  key_links:
    - from: "src/components/home/FeaturedProductsSection.tsx"
      to: "src/lib/types/product.ts"
      via: "import Product type"
      pattern: "import.*Product.*from.*types/product"
    - from: "src/lib/mock/products.ts"
      to: "src/lib/types/product.ts"
      via: "Product interface for all mock entries"
      pattern: "Product\\[\\]"
---

<objective>
Create the canonical Product type and expand mock data to cover all 6 catalog products.

Purpose: Every Phase 3 component depends on a shared Product type. The mock data must be complete (all 6 products with material and created_at fields) before catalog UI can be built. FeaturedProductsSection must be updated to use the new type without breaking the homepage.
Output: src/lib/types/product.ts (new), src/lib/mock/products.ts (updated), homepage components updated to import from new type location.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-product-catalog/03-CONTEXT.md
@.planning/phases/03-product-catalog/03-RESEARCH.md

@src/lib/mock/products.ts
@src/components/home/FeaturedProductsSection.tsx
@src/components/home/ProductCard.tsx
@src/components/home/AddToCartModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create canonical Product type at src/lib/types/product.ts</name>
  <files>src/lib/types/product.ts</files>
  <action>
    Create a new file src/lib/types/product.ts with three exports:

    1. ProductVariant interface: { id: string; name: string; price: number; in_stock: boolean }
    2. ProductMaterial type union: 'Gold' | 'Silver' | 'Crystal' | 'Tools'
    3. Product interface: {
         id: string
         name: string
         slug: string
         description: string
         image: string
         material: ProductMaterial
         is_featured: boolean
         variants: ProductVariant[]
         price_min: number
         price_max: number
         created_at: string   // ISO 8601 string — from Supabase or static mock value
       }

    This type must exactly match the Supabase products table schema from RESEARCH.md.
    The MockProductVariant and MockProduct interfaces in products.ts will be replaced by these.
  </action>
  <verify>npx tsc --noEmit passes with no type errors after file is created</verify>
  <done>src/lib/types/product.ts exists with Product, ProductVariant, ProductMaterial exported; no TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 2: Replace MockProduct with Product in mock data — add all 6 catalog products</name>
  <files>src/lib/mock/products.ts</files>
  <action>
    Rewrite src/lib/mock/products.ts:

    1. Remove MockProductVariant and MockProduct interface definitions (they now live in src/lib/types/product.ts).
    2. Import Product from '@/lib/types/product'.
    3. Keep the existing 4 products (24K Gold Beads, Gold Beads, Silver Beads, Onyx Beads) and add material + created_at to each.
    4. Add 2 missing products: Crystal Clear Beads and Shears.

    Material assignments (per CONTEXT.md):
    - 24K Gold Beads → Gold
    - Gold Beads → Gold
    - Silver Beads → Silver
    - Onyx Beads → Crystal
    - Crystal Clear Beads → Crystal
    - Shears → Tools

    created_at ISO strings (descending order — 24K Gold newest, Shears oldest):
    - 24K Gold Beads: '2024-06-01T10:00:00.000Z'
    - Gold Beads: '2024-05-15T10:00:00.000Z'
    - Silver Beads: '2024-04-20T10:00:00.000Z'
    - Onyx Beads: '2024-03-10T10:00:00.000Z'
    - Crystal Clear Beads: '2024-02-14T10:00:00.000Z'
    - Shears: '2024-01-01T10:00:00.000Z'

    Crystal Clear Beads (id: prod_005):
    - slug: crystal-clear-beads
    - description: "Transparent crystal loc beads — catches the light beautifully."
    - image: /images/products/placeholder-bead.svg
    - is_featured: false
    - variants: Small (4mm) ₦1600 in_stock: true, Medium (6mm) ₦2200 in_stock: true, Large (8mm) ₦2800 in_stock: true
    - price_min: 1600, price_max: 2800

    Shears (id: prod_006):
    - slug: shears
    - description: "Professional loc shears — precision-crafted for clean, sharp cuts."
    - image: /images/products/placeholder-bead.svg
    - is_featured: false
    - variants: Single variant — id: var_006a, name: "Standard", price: 3500, in_stock: true
    - price_min: 3500, price_max: 3500

    Export two arrays:
    - FEATURED_PRODUCTS: Product[] — the original 4 (is_featured: true), preserving homepage compatibility
    - CATALOG_PRODUCTS: Product[] — all 6 products, ordered with newest first (24K Gold → Gold → Silver → Onyx → Crystal Clear → Shears)

    Re-export MockProduct as a type alias for backward compatibility with any other files that import it:
    export type { Product as MockProduct } from '@/lib/types/product'

    Wait — check if AddToCartModal and ProductCard import MockProduct. If they do, they need updating too (handle in Task 3).
  </action>
  <verify>npx tsc --noEmit — zero errors; CATALOG_PRODUCTS.length === 6 verifiable by reading the file</verify>
  <done>
    - products.ts imports Product from types/product
    - CATALOG_PRODUCTS has 6 entries, all with material and created_at
    - FEATURED_PRODUCTS has 4 entries (the is_featured: true subset)
    - No TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Update homepage components to import from new type location</name>
  <files>
    src/components/home/ProductCard.tsx
    src/components/home/AddToCartModal.tsx
    src/components/home/FeaturedProductsSection.tsx
  </files>
  <action>
    FeaturedProductsSection.tsx, ProductCard.tsx, and AddToCartModal.tsx currently import MockProduct from '@/lib/mock/products'. Update each to import Product from '@/lib/types/product' and rename all MockProduct references to Product in their interfaces and function signatures.

    ProductCard.tsx:
    - Change: import { MockProduct } from '@/lib/mock/products'
    - To: import { Product } from '@/lib/types/product'
    - Rename ProductCardProps.product type from MockProduct to Product

    AddToCartModal.tsx:
    - Read file first to confirm it imports MockProduct
    - Same pattern: import Product from types/product, rename type references

    FeaturedProductsSection.tsx:
    - Change: import { MockProduct } from '@/lib/mock/products'
    - To: import { Product } from '@/lib/types/product'
    - Rename FeaturedProductsSectionProps.products type from MockProduct[] to Product[]
    - Rename useState<MockProduct | null> to useState<Product | null>

    The mock/products.ts backward-compat re-export (export type { Product as MockProduct }) handles any indirect imports we may have missed.

    After edits, run: npm run build
    The homepage must build successfully. No runtime behavior changes — this is a pure type rename.
  </action>
  <verify>npm run build completes with no TypeScript errors; npm run dev loads homepage at localhost:3000 with no console errors</verify>
  <done>All homepage components import from @/lib/types/product; build passes; homepage renders 4 featured products correctly</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit passes (zero errors)
- npm run build passes
- src/lib/types/product.ts exports Product, ProductVariant, ProductMaterial
- src/lib/mock/products.ts exports CATALOG_PRODUCTS (6) and FEATURED_PRODUCTS (4)
- All 6 products have: id, name, slug, description, image, material, is_featured, variants, price_min, price_max, created_at
- No homepage components reference MockProduct (except the backward-compat re-export in products.ts)
</verification>

<success_criteria>
- Product type is the canonical type for Phase 3+ — one source of truth at src/lib/types/product.ts
- CATALOG_PRODUCTS has all 6 products with correct material assignments and created_at strings
- Homepage builds and renders without errors — FeaturedProductsSection still shows 4 products
- TypeScript is satisfied throughout
</success_criteria>

<output>
After completion, create `.planning/phases/03-product-catalog/03-01-SUMMARY.md`
</output>
