---
phase: 12-build-a-notification-system-that-notifies-the-admin-email-or
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - drizzle/
autonomous: false
requirements:
  - NOTIF-01
  - NOTIF-02
user_setup:
  - service: database
    why: "Required to run `npx drizzle-kit push` against Postgres"
    env_vars:
      - name: DATABASE_URL_UNPOOLED
        source: ".env (preferred, per drizzle.config.ts) or shell env"
      - name: DATABASE_URL
        source: ".env or shell env (fallback)"
must_haves:
  truths:
    - "When a Paystack charge.success webhook creates an order, the system can record notification delivery state for that order"
    - "Notification state is idempotent per (order, channel) and prevents duplicate sends on webhook retries"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Drizzle table definition for order_notifications with a unique (order_id, channel)"
      min_lines: 10
    - path: "drizzle/"
      provides: "Generated Drizzle migrations / snapshot reflecting order_notifications"
      min_lines: 1
---

<objective>
Add a durable, idempotent notification-state layer in the database so webhook-triggered admin notifications can be retried safely without duplicates.

Purpose: Webhook retries + transient provider failures require persisted state; otherwise notifications can be duplicated or permanently missed.
Output: `order_notifications` Drizzle schema + pushed migration artifacts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-build-a-notification-system-that-notifies-the-admin-email-or/12-RESEARCH.md
@src/db/schema.ts
@src/app/api/webhooks/paystack/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `order_notifications` table to Drizzle schema</name>
  <read_first>
    - src/db/schema.ts
    - src/app/api/webhooks/paystack/route.ts
  </read_first>
  <files>
    - src/db/schema.ts
  </files>
  <action>
    In `src/db/schema.ts`, add a new Drizzle table named `orderNotifications` mapped to SQL table `order_notifications` with these concrete columns and constraints:

    - `id`: uuid PK defaultRandom()
    - `orderId`: uuid NOT NULL FK references `orders.id` with `onDelete: 'cascade'`
    - `channel`: text NOT NULL (store literal values `'email'` and `'whatsapp'`)
    - `status`: text NOT NULL default `'pending'` (store literal values `'pending' | 'sent' | 'failed'`)
    - `attempts`: integer NOT NULL default 0
    - `lastError`: text nullable (last provider error string)
    - `sentAt`: timestamptz nullable (`timestamp(..., { withTimezone: true })`)
    - `createdAt`: timestamptz NOT NULL defaultNow()
    - `updatedAt`: timestamptz NOT NULL defaultNow()

    Add a UNIQUE constraint or unique index enforcing idempotency across (`orderId`, `channel`).

    Implementation notes:
    - Use `pgTable(...)` and `uuid/text/integer/timestamp` from `drizzle-orm/pg-core` (add any missing imports, e.g. `uniqueIndex` or `unique` as needed).
    - Keep naming consistent with existing tables (camelCase column property names, snake_case SQL column names).
  </action>
  <acceptance_criteria>
    - `src/db/schema.ts` contains `pgTable('order_notifications'` and an exported `orderNotifications`
    - `src/db/schema.ts` includes `unique` enforcement for `(order_id, channel)` (grep for `order_id` and `channel` in the unique/index block)
    - `npm run lint` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mac/Documents/GitHub/twinkle && npm run lint</automated>
  </verify>
  <done>
    The project compiles and exports a Drizzle table definition for `order_notifications` with the specified columns and uniqueness guarantee.
  </done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Push Drizzle schema changes to the database</name>
  <read_first>
    - src/db/schema.ts
  </read_first>
  <files>
    - drizzle/
  </files>
  <action>
    Run the Drizzle push command from the repo root:

    - `npx drizzle-kit push`

    Requirements:
    - Ensure `DATABASE_URL_UNPOOLED` or `DATABASE_URL` is set in the environment so the command can connect non-interactively.
    - If `drizzle-kit push` prompts for confirmation (non-TTY), STOP and convert this plan to `autonomous: false` during execution, then rerun after resolving prompts.
  </action>
  <acceptance_criteria>
    - A new migration/snapshot reflecting `order_notifications` exists under `drizzle/` (grep for `order_notifications` within `drizzle/`)
    - The push command exits successfully (0)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/mac/Documents/GitHub/twinkle && npx drizzle-kit push</automated>
  </verify>
  <done>
    Database schema is updated and the repo contains the generated Drizzle artifacts for `order_notifications`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Paystack → `/api/webhooks/paystack` | Untrusted webhook payload enters the system; even with signature verification, duplicates/retries are expected |
| App → Database | Writes must be idempotent to prevent duplicate notifications |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | D | Webhook retry behavior | mitigate | Add `order_notifications` with unique `(order_id, channel)` to prevent duplicate sends and enable safe retries |
| T-12-02 | T | Notification state tampering | mitigate | Store state server-side only; enforce uniqueness constraint at DB level (not in-memory flags) |
| T-12-03 | R | Missing auditability of send attempts | mitigate | Persist `attempts`, `status`, `last_error`, `sent_at` per order/channel |
</threat_model>

<verification>
- `npm run lint` passes
- `npx drizzle-kit push` completes and `drizzle/` contains `order_notifications` artifacts
</verification>

<success_criteria>
- `order_notifications` exists in Drizzle schema with the required columns and unique `(order_id, channel)` constraint
- The database has been pushed and Drizzle artifacts reflect the new table
</success_criteria>

<output>
After completion, create `.planning/phases/12-build-a-notification-system-that-notifies-the-admin-email-or/12-01-SUMMARY.md`
</output>

