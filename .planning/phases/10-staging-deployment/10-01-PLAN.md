---
phase: 10-staging-deployment
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/webhooks/paystack/route.ts
  - src/app/orders/[reference]/page.tsx
  - src/app/shipping/page.tsx
  - src/components/about/AboutSection.tsx
autonomous: true

must_haves:
  truths:
    - "No file in the codebase reads process.env.NEXT_PUBLIC_PAYSTACK_SECRET_KEY"
    - "SUPABASE_SERVICE_ROLE_SECRET is replaced by SUPABASE_SERVICE_ROLE_KEY everywhere"
    - "No hardcoded placeholder 2348000000000 exists in any source file"
    - "sitemap.ts staticRoutes covers all public routes added through Phase 9"
  artifacts:
    - path: "src/app/api/webhooks/paystack/route.ts"
      provides: "Paystack webhook handler"
      contains: "SUPABASE_SERVICE_ROLE_KEY"
    - path: "src/app/orders/[reference]/page.tsx"
      provides: "Order confirmation server component"
      contains: "SUPABASE_SERVICE_ROLE_KEY"
    - path: "src/app/shipping/page.tsx"
      provides: "Shipping public page"
      contains: "BUSINESS.whatsapp"
    - path: "src/components/about/AboutSection.tsx"
      provides: "About section component"
      contains: "BUSINESS.whatsapp"
    - path: "src/app/sitemap.ts"
      provides: "Dynamic sitemap"
      contains: "/newsletter"
  key_links:
    - from: "src/app/api/webhooks/paystack/route.ts"
      to: "SUPABASE_SERVICE_ROLE_KEY env var"
      via: "process.env.SUPABASE_SERVICE_ROLE_KEY"
      pattern: "SUPABASE_SERVICE_ROLE_KEY"
    - from: "src/app/shipping/page.tsx"
      to: "BUSINESS.whatsapp.url()"
      via: "import from @/lib/config/business"
      pattern: "BUSINESS\\.whatsapp"
---

<objective>
Fix all pre-deployment code bugs that would cause runtime failures or security issues on Vercel before the first build.

Purpose: A clean build with correct env var names and no hardcoded placeholders is required before Vercel deployment. These bugs cannot be patched after the fact — NEXT_PUBLIC_ variables are baked at build time, wrong env var names cause 500s at runtime, and hardcoded test numbers look unprofessional in production.

Output: Four files updated, TypeScript build passes cleanly (`npm run build`).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-staging-deployment/10-RESEARCH.md
@src/lib/config/business.ts
@src/lib/supabase/admin.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate SUPABASE_SERVICE_ROLE_SECRET to SUPABASE_SERVICE_ROLE_KEY</name>
  <files>
    src/app/api/webhooks/paystack/route.ts
    src/app/orders/[reference]/page.tsx
  </files>
  <action>
Two source files use the wrong env var name `SUPABASE_SERVICE_ROLE_SECRET` instead of `SUPABASE_SERVICE_ROLE_KEY` (the canonical name used in src/lib/supabase/admin.ts).

In `src/app/api/webhooks/paystack/route.ts` line 86:
- Change `process.env.SUPABASE_SERVICE_ROLE_SECRET!` to `process.env.SUPABASE_SERVICE_ROLE_KEY!`

In `src/app/orders/[reference]/page.tsx` line 13:
- Change `process.env.SUPABASE_SERVICE_ROLE_SECRET!` to `process.env.SUPABASE_SERVICE_ROLE_KEY!`

Do NOT change the createClient call structure in either file — only the env var name changes. These files inline their own createClient (with auth.persistSession:false) rather than using createAdminClient() — leave that pattern intact.
  </action>
  <verify>
    grep -r "SUPABASE_SERVICE_ROLE_SECRET" src/ — must return no matches
    grep -r "SUPABASE_SERVICE_ROLE_KEY" src/ — must return 3 matches (admin.ts + route.ts + orders page)
  </verify>
  <done>Zero references to SUPABASE_SERVICE_ROLE_SECRET in the codebase. All three service-role usages reference SUPABASE_SERVICE_ROLE_KEY.</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded 2348000000000 with BUSINESS.whatsapp</name>
  <files>
    src/app/shipping/page.tsx
    src/components/about/AboutSection.tsx
  </files>
  <action>
Two files bypass the BUSINESS.whatsapp single source of truth and hardcode the placeholder number 2348000000000.

In `src/app/shipping/page.tsx`:
- Add import at the top: `import { BUSINESS } from '@/lib/config/business'`
- Replace line 56:
  ```ts
  const waUrl = `https://wa.me/2348000000000?text=${encodeURIComponent(raw.shipping_intl_message)}`
  ```
  with:
  ```ts
  const waUrl = BUSINESS.whatsapp.url(raw.shipping_intl_message)
  ```
  This is correct because BUSINESS.whatsapp.url(message) already builds the wa.me?text= URL.

In `src/components/about/AboutSection.tsx`:
- Add import at the top: `import { BUSINESS } from '@/lib/config/business'`
- Replace the hardcoded href on line 39:
  ```tsx
  href="https://wa.me/2348000000000"
  ```
  with:
  ```tsx
  href={BUSINESS.whatsapp.url()}
  ```

Do not change any other part of either file.
  </action>
  <verify>
    grep -r "2348000000000" src/ — must return no matches
    npm run build — must complete without TypeScript errors
  </verify>
  <done>No hardcoded WhatsApp placeholder remains. Both files use BUSINESS.whatsapp.url() from the config singleton. Build passes clean.</done>
</task>

<task type="auto">
  <name>Task 3: Audit and complete sitemap.ts staticRoutes</name>
  <files>
    src/app/sitemap.ts
  </files>
  <action>
The current staticRoutes array in src/app/sitemap.ts covers: /, /catalog, /about, /faq, /shipping, /blog.

Review all public-facing routes added through Phase 9 by scanning:
- src/app/ for page.tsx files not under (admin)/, api/, orders/, cart/ (private/transactional routes)

Expected public routes to verify are included:
- / ✓ already present
- /catalog ✓ already present
- /about ✓ already present
- /faq ✓ already present
- /shipping ✓ already present
- /blog ✓ already present
- /cart — intentionally EXCLUDED (transactional, no SEO value; already disallowed in robots.ts)
- /orders/[reference] — intentionally EXCLUDED (private, no SEO value)

If any public informational route is missing from staticRoutes, add it with appropriate changeFrequency and priority following the existing pattern.

If the audit confirms all public routes are already present, add a single comment line above staticRoutes:
```ts
// Audited 2026-04-01: all public routes present. /cart and /orders/ are intentionally excluded.
```
This documents that the audit was done so future developers know it was intentional.
  </action>
  <verify>
    npm run build — sitemap.ts must compile without errors
    Review the staticRoutes array and confirm no public page.tsx exists outside the list
  </verify>
  <done>sitemap.ts staticRoutes is complete and audited. All public informational routes are included. Transactional routes (/cart, /orders/) are deliberately excluded. Audit comment added.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `grep -r "SUPABASE_SERVICE_ROLE_SECRET" src/` returns no output
2. `grep -r "2348000000000" src/` returns no output
3. `npm run build` completes with exit code 0 (no TypeScript errors, no missing imports)
4. `grep -r "NEXT_PUBLIC_PAYSTACK_SECRET_KEY" src/` returns no output (confirm this was never in source code, only .env.local — note in summary if confirmed absent)
</verification>

<success_criteria>
- All three env var consolidation changes committed
- Both hardcoded placeholder WhatsApp numbers replaced with BUSINESS.whatsapp.url()
- sitemap.ts audit complete with comment
- `npm run build` passes clean
- No references to SUPABASE_SERVICE_ROLE_SECRET or 2348000000000 remain in src/
</success_criteria>

<output>
After completion, create `.planning/phases/10-staging-deployment/10-01-SUMMARY.md` with:
- Files modified
- Build verification result
- Confirmation that NEXT_PUBLIC_PAYSTACK_SECRET_KEY is absent from source (only .env.local)
- Any additional env var issues found during audit
</output>
